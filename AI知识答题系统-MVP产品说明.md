- 版本：v0.14（MVP）
- 日期：2026-09-23
- 状态：**功能闭环 + 后端（账号/同步）+ 每日提醒均已上线**（2026-09-23，T-033/T-034）；题库 315 题 + 203 道待确认版权的简答草稿；✅ 后端全链路冒烟通过（健康 → 注册/登录 → `/auth/me` → 进度读写），前端静态产物已重新上传且与本地 `dist` 逐字节一致；⚠️ **手机实测仍未做**
- 本版变更：一次**架构决策反转**（T-026~T-031）。用户明确「自己用、不碰备案」，于是把 v0.3 的「纯静态、不做账号、不买服务器」推翻：新增 **CloudBase HTTP 云函数 + PostgreSQL** 做注册登录与按账号隔离的进度同步（本地优先，离线照常答题）；另新增**每日提醒**，做法是导出 `.ics` 交给手机自带日历按天响铃（零后端、零推送）。反转的理由、边界与遗留风险见 10.12 / 10.13，完整记录见附录 A.1

---

## 0. 交接摘要（**下一个 Agent 先读这一章**）

> 这一章是「项目当前真实状态」的权威来源。第 1–13 章是产品设计与规划。**注意：原第 4 章「进 MVP」列有 13 处与代码不符，已在 v0.5 修正为真实状态。**若第 1–13 章与本章冲突，以本章为准。

### 0.0 接手须知（30 秒版）

1. **目标**：给 AI 从业者一个「每天 10 题」的工程知识巩固工具，靠「每日任务感 + streak + 长期掌握度画像」立足（见 0.1）。
2. **当前进度**：功能闭环已完成（0.3），题库 315 题；v0.14 的**后端（注册登录 + 跨设备同步）**与**每日提醒**已**全量上线**（T-033 部署后端 + T-034 部署前端，2026-09-23），线上页面已能看到登录与提醒入口。剩下的验收是**手机 4G 实测**。
3. **怎么改代码**：先读 `AGENTS.md`（分层 / 计划 / 800 行提交 / 只跑相关测试 / 完成即提交），任务清单在 `docs/PLAN.md`。
4. **加题**：`tools/gen-questions.mjs --draft` 出草稿 → 人工审 → `--merge` 合并（见 0.5）。
   合并会重排 id，**必须同步 `app/src/data/questions/all.test.ts` 里 `EXPECTED_FILES` 的题量计数**（v0.11 起该文件锁的是「每文件题量 + 分块升序」，不再逐条列 id）。
   合并逻辑已按 id 升序落盘，不会因为填补空号而打乱顺序。
   （v0.10 起可先用 `node tools/pipeline/run.mjs --all` 从 6 个权威开源项目自动抓素材出草稿，见 0.5）
5. **改后端 / 部署后端**：后端全在 `cloud/functions/api/`（事件云函数 + HTTP 访问服务 + CloudBase PostgreSQL），本机用 `node tools/backend.mjs <secret|apikey|migrate|deploy|smoke|status>` 操作；**密钥不入库**（生成到已 gitignore 的 `cloudbaserc.local.json` / `.secrets/`）。改前先读 `AGENTS.md` 第 9 节——尤其是「为什么要 API Key」那一节。
6. **标准收尾**：`node tools/finish-task.mjs --task T-0NN --message "feat(scope): 说明" --evidence "验证方式"`。

### 0.1 项目是什么

面向 AI 从业者的**每天 10 题**工程知识巩固系统：题库按知识域分类，答完即时给**分层解析**（正确答案为什么对 + 每个干扰项错因 + 延伸知识点），并可**溯源到一手资料**，长期沉淀个人掌握度画像。

**差异化主张（第 2.4 节，三条必须全部做到）**

1. 每日任务感（每天固定 10 题，3–5 分钟可完成）
2. streak 连续打卡
3. 跨会话的长期掌握度画像

靠这三点与「面试鸭（求职八股）」「Gemini Notebook（免费替代品）」错位竞争。

**明确不做**：微信小程序、原生 App、付费体系、面试题库。

> ⚠️ **v0.14 反转**：「服务器」与「账号体系」原先在「明确不做」里，现已**改为要做并已实现**（用户自己用、不碰备案）。原因见 10.12。

### 0.2 已定路线（不可逆决策，不要再重新比较方案）

| 决策 | 结论 | 依据 |
| --- | --- | --- |
| 技术栈 | Vite + React + TypeScript + 原生 CSS | 已实现并上线 |
| 系统形态 | **Web PWA 本地优先 + CloudBase HTTP 云函数后端**（v0.14 起） | 第 10.1 / 10.12 节 |
| 托管 | 腾讯云 CloudBase 静态网站托管（体验版，¥0） | 第 10.3 节 |
| 微信小程序 | **已评估并否决**（request 域名需备案 HTTPS，个人主体类目受限） | `docs/research/小程序调研.md` |
| 账号体系 | **v0.14 反转：要做**。注册登录 + 自签会话令牌，进度/错题按账号隔离 | 第 10.12 节 |
| 数据存储 | 浏览器 localStorage 为主（离线优先）+ CloudBase PostgreSQL 作同步副本；**题库不入库**，仍留仓库 JSON | 第 7.2 / 10.12 节 |
| 每日提醒 | **v0.14 新增**：导出 `.ics` 交给手机自带日历按天响铃（等于闹钟），零后端零推送 | 第 10.13 节 |
| 模型批阅 key | 仍然只存本机 `aq.agent.v1`，**不上云、不进同步、不进进度码** | 第 0.3 / 10.12 节 |

### 0.3 当前进度（核实日期 2026-09-23）

| 项 | 状态 | 备注 |
| --- | --- | --- |
| 前端骨架（页面 + 底部导航） | ✅ 已完成 | 今日 / 复习 / 分类 / 我的数据 四个 Tab，共 7 个页面组件 |
| 题库数据模型 + **315 道题** | ✅ 已完成 | 源数据为 JSON；RAG / Agent / 本体各 100 题（T-020 / T-021 / T-022），见 0.5 |
| 每日推荐规则引擎 | ✅ 已完成 | `app/src/lib/recommend.ts` |
| 答题流程 / 即时反馈 / 分层解析 | ✅ 已完成 | 含**出处原文片段**（v0.6 补齐） |
| **题型：判断 + 简答（T-018）** | ✅ 已完成 | 判断题恰好 2 个选项；简答题无选项、靠 `referenceAnswer` + `keyPoints` 批阅 |
| **简答批阅：Agent + 本地兜底（T-019）** | ✅ 已完成 | 纯静态站无后端：用户在设置里自带 API key（存本机 `aq.agent.v1`，**不参与进度码导出**），浏览器直连 OpenAI 兼容接口；未配 key 或调用失败自动降级为**本地要点覆盖率评分** |
| **查看答案即未掌握（T-019）** | ✅ 已完成 | 显式确认后记 `viewedAnswer: true` + `isCorrect: false`，熟练度置为「未掌握」 |
| **熟练度三档自评（T-018）** | ✅ 已完成 | 已掌握 / 模糊 / 未掌握；用户显式自评优先于自动推断 |
| **艾宾浩斯遗忘曲线推荐（T-018）** | ✅ 已完成 | 间隔 1/2/4/7/15/30/60 天，按熟练度缩放；保持率 `R = e^(-Δt/S)`，按「忘得最狠优先」排序 |
| **分类专项练习页（A7）** | ✅ 已完成 | v0.9 新增 `pages/Categories.tsx` + `lib/categories.ts` |
| **断点续答（A9）** | ✅ 已完成 | v0.9：答完即记进度，重进回到中断的那一题（`lib/task.ts`） |
| **收藏题目（B3 上半）** | ✅ 已完成 | v0.9：答题页可收藏，复习页「收藏」标签可查看与练习 |
| **离线出题流水线（D3/D8）** | ✅ 已完成 | v0.9 新增 `tools/gen-questions.mjs`：--check / --draft / --merge |
| **题目获取流水线（T-013）** | ✅ 已完成 | v0.10 新增 `tools/pipeline/`：6 个权威开源源 → CorpusDoc → chunk → 草稿；结构化日志 + 运行报告；与业务解耦 |
| **笔记型仓库抽题（T-023）** | ✅ 已完成（草稿阶段） | v0.12 新增 `tools/extract-notes.mjs` + `tools/pipeline/notes.mjs`：解析「标题即问题、正文即答案」的面试笔记仓库，抽出 **203 道简答草稿**（已过契约校验）；**未入库**——源仓库无 LICENSE，按 `AGENTS.md` 第 8 节需先确认使用口径 |
| 题目反馈按钮（B4） | ✅ 已完成 | 4 种原因，存本地 `aq.reports.v1`（v0.6 新增） |
| 错题本 + 间隔重复 | ✅ 已完成 | v0.11 起由 `app/src/lib/ebbinghaus.ts`（艾宾浩斯调度）+ `mastery.ts`（熟练度）取代旧的固定间隔表 `review.ts`（该文件已删除） |
| 统计页 + 掌握度图 + streak | ✅ 已完成 | `app/src/pages/Stats.tsx` |
| 进度导出码 / 导入（B6） | ✅ 已完成 | 明文 base64；v0.9 新增口令加密（AES-GCM + PBKDF2，`AQ1.` 前缀） |
| PWA（manifest + Service Worker） | ✅ 已完成 | v0.9 补齐 192/512 PNG + maskable + apple-touch-icon（iOS 可用） |
| 构建 `npm run build`（含 `tsc --noEmit`） | ✅ 通过 | 2026-09-22 复验无报错 |
| 部署到 CloudBase 静态托管 | ✅ 已完成 | 2026-09-22 复核：线上 12 个文件与本地 `dist` **逐字节一致**（sha256 比对），`index-BaGNxCyh.js`，图标全 200；并清掉 5 个历史遗留旧产物 |
| **后端：账号 + 同步（T-026~T-033）** | ✅ 已完成并部署 | `cloud/functions/api/`：注册、登录、改密、`/auth/me` + `/progress` 按账号隔离读写（乐观并发，冲突返 409 带最新值）；前端 `lib/api.ts` / `session.ts` / `account.ts` / `sync.ts` + 「我的」页 + 底部第 5 个 Tab。云函数 **41** 个用例通过；线上全链路冒烟通过（T-033） |
| **每日提醒（T-031）** | ✅ 已完成 | 导出 `.ics`（`lib/reminder.ts` + `pages/ReminderCard.tsx`）：`VTIMEZONE` + `RRULE:FREQ=DAILY` + `VALARM`，手机日历到点响铃；设置只存本机 `aq.reminder.v1`，24 个用例 |
| **手机 4G/5G 真实网络实测** | ⏳ **未完成（唯一卡住的一步）** | 清单见第 10.7 节 |
| 题库扩容（T-002 / T-020 / T-021 / T-022） | ✅ 已完成 | 全库 **315 题**；RAG / Agent / 本体各 100 题；工程判断题（`isPractice`）**99 道**，满足 T-002 的 ≥60 验收下限 |
| 题目笔记（B3 下半） | ❌ 未做 | 收藏已做，个人笔记未做 |
| 管理后台 / 批量导入界面（D1） | ❌ 不做 | 由 `tools/gen-questions.mjs` 的 CLI 流水线替代 |
| README.md | ✅ 已完成 | 指向第 0 章交接摘要（v0.6 新增） |
| **开发约束（agent harness）** | ✅ 已建立 | `AGENTS.md` + `tools/`：分层、计划、行数、测试四道门禁（v0.8） |
| git 仓库 | ✅ 已建立 | 远程 `https://github.com/Htojk/ai_drill.git`（v0.7） |
| 自动化测试 | ✅ 已有 | **496 个用例**（vitest，17 个文件）+ **云函数 35 个**（`node --test`）+ 流水线 127 个，命令见 `AGENTS.md` |
| **CI** | ✅ 已完成 | v0.9 新增 `.github/workflows/ci.yml`（题库校验 → 分层 → 构建 → 全量测试），首次运行已通过 |
| 流水线单测 | ✅ 已扩充 | `node --test "tools/pipeline/*.test.mjs"` 共 **127 个用例**（v0.12 新增 notes 34 个） |

### 0.4 关键文件地图

| 路径 | 作用 |
| --- | --- |
| `README.md` | 项目入口，指向本文档第 0 章（v0.6 新增） |
| `AGENTS.md` | **开发约束（harness）：分层规则、提交流程、门禁命令（v0.8 新增）** |
| `docs/PLAN.md` | 任务计划；已完成只留 5 条，更早的归档到 `docs/plan-archive/` |
| `docs/research/` | 调研笔记：竞品（2 篇）、小程序否决、CloudBase 默认域名风控（v0.9 从根目录移入） |
| `tools/` | 约束脚本 5 个（plan / check-commit-size / check-layers / test-related / finish-task）+ 内容脚本与共享模块（gen-icons / gen-questions / question-schema / llm / **extract-notes（v0.12）**） |
| `cloud/functions/api/` | **后端云函数（v0.14）**：`index.mjs`（入口 + 事件适配 + 路由）→ `routes/`（endpoint）→ `repo/`（PostgREST 读写）→ `core/`（口令 scrypt、自签令牌、日志、校验）。分层靠目录约定，单文件 ≤250 行 |
| `cloud/functions/api/schema.sql` | 后端表结构：`drill_accounts` / `drill_progress`；**刻意不授权给 anon / authenticated**；云函数要带 `DRILL_API_KEY`（JWT 里 `role=service_role`）才能读写 |
| `tools/backend.mjs` | 后端运维 CLI：`secret`（会话密钥）/ `apikey`（数据库 API Key）→ `migrate`（建表）→ `deploy`（部署函数）→ `smoke`（真实 HTTP 全链路冒烟）→ `status` |
| `cloudbaserc.json` | CloudBase 函数声明（函数名 `api`、`type: Event`、Nodejs20.19）；HTTP 入口靠 `fn deploy --path /api` 的访问服务挂出来；密钥走 `cloudbaserc.local.json`（已 gitignore） |
| `content/drafts/`（已 gitignore） | 出题草稿；v0.11 的 315 题就是分批写草稿 → `--merge` 入库的 |
| `tools/pipeline/` | **内容流水线**：`run.mjs` 抓权威源（v0.10）→ `normalize.mjs` / `chunk.mjs` / `stages.mjs`；**`notes.mjs` 解析笔记型仓库（v0.12，纯函数，配套 34 个用例）**。各文件 ≤ 320 行 |
| `content/`（`raw` / `corpus` / `chunks` / `logs` / `reports` 子目录） | 流水线中间产物（可重建，已 gitignore）：原始素材 → CorpusDoc → 切块 → JSONL 日志 → 运行报告 |
| `.github/workflows/ci.yml` | CI：题库校验 → 分层检查 → 构建 → 全量测试（v0.9 新增） |
| `AI知识答题系统-MVP产品说明.md` | 本文档（产品设计 + 当前状态） |
| `手机访问二维码.png` | 指向线上地址的二维码（410×410 有效 PNG） |
| `app/src/data/questions/*.json` | **题库源数据：每个分类一个 JSON（各约 145 行），`index.ts` 断言成 `Question[]` 并组装** |
| `content/sources/` | 出题素材（论文摘要/文档段落），供 `gen-questions.mjs --draft` 使用 |
| `tools/gen-questions.mjs` | 出题流水线：草稿写在 `content/drafts/`（已 gitignore），合并前会校验不变量 |
| `app/src/lib/recommend.ts` | 每日推荐规则引擎 |
| `app/src/lib/ebbinghaus.ts` | **艾宾浩斯复习调度**：间隔表 1/2/4/7/15/30/60 天，按熟练度缩放，计算保持率与到期判断（v0.11） |
| `app/src/lib/mastery.ts` | **熟练度三档**（已掌握 / 模糊 / 未掌握）的存储与合并规则：显式自评优先于自动推断（v0.11） |
| `app/src/lib/grading.ts` | **简答本地评分**：中文按「相邻二字」token、英文按单词做模糊匹配，算要点命中率 → score / verdict（v0.11） |
| `app/src/lib/agent.ts` | **模型批阅**：自带 key 直连 OpenAI 兼容 `/chat/completions`；失败或未配置时自动降级本地评分（v0.11） |
| `app/src/lib/agent-store.ts` | 批阅配置的本机存储（`aq.agent.v1`，**不进进度导出码**）（v0.11） |
| `app/src/lib/question-kind.ts` | 题型判定与展示文案（单选 / 判断 / 简答 / 场景） |
| `app/src/lib/task.ts` | 当天任务的断点续答（第一道没做的题 / 标记完成） |
| `app/src/lib/categories.ts` | 分类练习的进度统计与出题顺序（未练 → 错题 → 已对） |
| `app/src/lib/crypto.ts` | 进度码口令加密（PBKDF2 + AES-GCM） |
| `app/src/lib/storage.ts` | localStorage 封装 + 整包读写（`readAll` / `writeAll`）+ 进度导出码 + 题目反馈存储 |
| `app/src/lib/merge.ts` | **同步合并规则**：整包按字段合并 + `isSamePayload` 规范化比较，避免无谓上传（v0.14） |
| `app/src/lib/api.ts` | 前端 HTTP 客户端：`ApiError`（含离线语义）、统一 `POST/GET/PUT /api/*`（v0.14） |
| `app/src/lib/session.ts` | 会话令牌的本机存储与登录态判断（v0.14） |
| `app/src/lib/account.ts` | 账号编排层：注册 / 登录 / 改密 / 登出 + 错误码翻译成人话（v0.14） |
| `app/src/lib/sync.ts` | **本地优先同步**：`pull` / `push`（409 时合并重投一次）/ 4 秒防抖上传 / 事件订阅（v0.14） |
| `app/src/hooks/use-sync.ts` | 把同步层接到 React：合并落地后重读本机数据（v0.14） |
| `app/src/lib/reminder.ts` | **每日提醒 ICS 生成**（纯函数：时区、RRULE、75 字节折行、文本转义）（v0.14） |
| `app/src/lib/stats.ts` | 掌握度统计 |
| `app/src/pages/*.tsx` | Today / Quiz / QuizExplanation / Result / WrongBook（含收藏标签）/ Categories / Stats / **ShortAnswerBox（简答输入）/ QuestionBody（题干与选项）/ MasteryPicker（熟练度选择）/ AgentSettings（批阅配置）/ NavBar / Account（我的页）/ AccountPanel（账号面板）/ LoginForm（登录注册）/ ReminderCard（每日提醒，v0.14）** |
| `app/src/types.ts` | 全部数据模型类型定义 |
| `app/vite.config.ts` | Vite 配置 + PWA manifest / workbox |
| `app/dist/` | 构建产物（部署的就是这个目录，共 12 个文件） |

**版本控制**：已托管到 `https://github.com/Htojk/ai_drill.git`（分支 `main`）。`node_modules` 与 `app/dist` 已在 `.gitignore` 中忽略。

### 0.5 题库现状（v0.11 核实）

- 总量 **315 题**，8 个分类文件：`rag` / `agent` / `ontology` 各 **100 题**，`base` / `prompt` / `tuning` / `eval` / `engineering` 仍是各 3 题。
  - 注：「分类计数」会略大于题量，因为**跨分类题会被每个分类各记一次**（例如 `ontology.json` 里有一道题同时挂 RAG）。
- **题型分布**：单选 71、判断 60、简答 180、场景 4。
  - 三个 100 题分类的配比完全一致：**20 单选 + 20 判断 + 60 简答**。
  - 判断题契约：**恰好 2 个选项**（A 正确 / B 错误），错误项必须写 `wrongReason`。
  - 简答题契约：`options: []` + 必填 `referenceAnswer`、可选 `keyPoints`（评分要点数组）。
- **工程判断题**（`isPractice`，UI 上标「工程判断」）：**99 道**，分布覆盖 Agent(34) / 本体(19) / RAG(16) 等；满足 `all.test.ts` 里 ≥60 且跨 ≥3 分类的不变量（T-002 验收口径）。
- **难度分布**：1 分 1 题、2 分 45 题、3 分 113 题、4 分 145 题、5 分 11 题。
- **来源类型**：`doc` 120、`paper` 111、`spec` 84；每题都必须有 `source.title / url(https) / snippet`。
- 不变量由 `tools/gen-questions.mjs --check` 与 `app/src/data/questions/all.test.ts` 双重把关：id 唯一、**每个文件 id 成块升序**（锁题量而非逐条列 id）、干扰项必须有 `wrongReason`、简答题必须有参考答案与要点、分类必须在白名单内。
- **出题流程**：`node tools/gen-questions.mjs --merge <草稿.json> <分类>`（草稿写在 `content/drafts/`，已 gitignore）。
  - 合并时按 id 升序落盘（v0.11 修复：取号会填补被删除的空号，旧实现只追加会把顺序打乱）。
  - **合并后必须同步 `all.test.ts` 里 `EXPECTED_FILES` 对应前缀的 count**，否则测试会红。
  - 单次提交 ≤800 行，所以一次 merge 建议控制在 **10 道单选/判断**或 **15 道简答**以内。
  - v0.10 的抓取流水线（`tools/pipeline/run.mjs --all`）依然可用，只是 v0.11 这批题是直接按领域撰写的，没有走抓取。
- **待入库的草稿（v0.12）**：`content/drafts/notes-llm_interview_note-<runId>-<分类>.json` 共 6 个文件、**203 道简答**（微调与对齐 84 / 大模型基础 54 / 工程与部署 33 / 评估与可观测 13 / 提示工程 10 / Agent 9）。
  - 来源是社区整理的面试笔记（`source.type: "community"`），草稿标签为 `["面试笔记", "待复核"]`。
  - **为什么没并进题库**：源仓库 `wdndev/llm_interview_note` 没有 LICENSE，默认版权保留；整段复制进公开站点有侵权风险。决策口径见 `AGENTS.md` 第 8 节。
  - 草稿在 `content/drafts/`（已 gitignore，可重建），重建命令：`node tools/extract-notes.mjs --local <笔记目录>`。

### 0.6 已知问题与技术债（v0.14 核实，按优先级）

| 优先级 | 问题 | 说明 |
| --- | --- | --- |
| ~~P0~~ | ~~前后端产物落后于本地~~ | ✅ **已解决（T-033 / T-034，2026-09-23）**：后端部署 + 冒烟通过；前端重新构建上传，线上 12 个文件与本地 `dist` 逐字节一致，旧产物已清理 |
| P0 | 手机实测尚未执行 | 第 10.7 节清单，是决定「要不要备案」的唯一依据。用户已表态「自己用」，所以默认接受默认域名的中间页 |
| ~~P0~~ | ~~线上产物落后于本地~~ | ✅ **已解决（v0.13）**：线上 12 个文件与本地 `dist` 逐字节一致 |
| P1 | 题库只做了 3 个重点领域 | RAG / Agent / 本体各 100 题，其余 5 个分类仍是占位级的 3 题（用户要求聚焦这三块） |
| P1 | Agent 批阅未做过真实模型验证 | 需要用户自带 API key 才能跑通模型批阅；目前只有单测覆盖，没有端到端实测记录 |
| P1 | **同步是「整包覆盖 + 乐观并发」，不是字段级合并** | 两台设备各自离线改同一道题，最终以 revision 更大的一方为准（合并规则见 `lib/merge.ts`）。自用够，但别指望它像 CRDT 那样两边都保住 |
| P1 | **注册是开放式的** | 云函数默认允许任何人注册（`DRILL_ALLOW_REGISTER=off` 可一键关闭）。自用场景建议注册完自己就关掉 |
| P1 | 会话令牌没有撤销列表 | 自签 HS256 令牌，改密会让旧令牌失效（密钥派生变了），但没有「逐条吊销某个设备」的能力；令牌有效期见 `cloud/functions/api/core/token.mjs` |
| P1 | 简答题评分偏「要点覆盖率」 | 中文按相邻二字 token 做模糊匹配，只能判断要点是否被提到，无法评价论述质量与逻辑。**v0.13 修掉了阈值标定错误**（详见 A.2），但「同义不同词」仍会漏判：例如要点写「OWA」而用户写「开放世界假设」，字面对不上就是 0 分。根治要靠模型批阅（配 key）或引入同义词表 |
| P1 | **203 道社区笔记草稿悬而未决** | 已抽好并通过契约校验，但源仓库无 LICENSE；合与不合都需要用户拍板（见 `AGENTS.md` 第 8 节）。**用户点头前不要 merge** |
| P2 | 题目笔记未做 | 收藏已做（v0.9），笔记仍是空档（B3 下半） |
| P2 | 明文进度码仍可导出 | 明文 base64 保留兼容性；加密导出需用户主动选择（v0.9 已有 AES-GCM） |
| P2 | 出题草稿质量依赖人工 | 离线草稿是占位模板；调模型出题需要自备 API key，且产出必须人工复核 |
| P2 | 流水线 `llm` 出题模式未实测 | T-013 只验证了离线模式；`--stage draft --mode llm` 尚未跑真实模型出题 |
| P2 | 流水线无增量缓存 | 每次 `fetch` 都重拉整棵文件树，暂未做「已抓过就跳过」 |
| P2 | 笔记抽题只能出简答题 | 笔记结构是「标题即问题、正文即答案」，无法可靠地自动生成干扰项，所以只产出 `short` 题；要补选择/判断题仍需人工或走 `pipeline` + 模型 |
| P2 | 页面文件接近行数上限 | `App.tsx`(229)、`lib/storage.ts`(215)、`pages/Quiz.tsx`(203) 已过 200 行告警线，>250 会被提交拦截，加功能前应先拆 |
| P2 | 云函数日志没有集中检索 | 结构化日志打在云函数输出里（CloudBase 控制台可查），但没有导出到本地或做告警；排查线上问题要手动翻 |
| P2 | 提醒只支持「一个每日时刻」 | 不支持工作日/周末区分、不支持多条提醒；要改时间得重新导出 .ics 并删掉旧日程（见 10.13） |
| P2 | 部署不会自动清理旧产物 | `tcb hosting deploy` 只上传、不删除，历史 `assets/*` 会一直堆积（本次手工清了 5 个）。部署后建议跑一次 `tcb hosting list /` 核对 |

### 0.7 建议的下一步（按顺序）

1. **手机实测**（第 10.7 节清单）：扫 `手机访问二维码.png` 走一遍静态功能，并补测登录与跨设备同步。前后端都已上线，这是目前唯一没做的验收。
2. （已完成）前后端部署见 0.3 / 10.5；如需再改前端，改完记得 `npm run build` + `tcb hosting deploy`（**部署前需用户点头**），并手工清理旧哈希产物。
3. **决定 203 道社区笔记草稿的去留**（v0.12 新增）：三个选项——(a) 不入库，仅留本地草稿；(b) 先按「仅本地/小范围使用」入库；(c) 改写表述后入库并标注来源。**没有用户明确表态就不要 merge。**
4. **验证 Agent 批阅**：在「设置」里填一个 OpenAI 兼容的 key，随便答一道简答题，确认模型批阅生效、断网/填错 key 时能降级到本地评分。
5. 之后可选：其余 5 个分类补题、题目笔记、随机模考、练习模式的掌握度筛选、CI 覆盖率门槛、`App.tsx` / `Quiz.tsx` 拆分。

### 0.8 怎么把它跑起来

```bash
cd app
npm install          # 首次
npm run dev          # 本地开发 http://localhost:5173
npm run build        # 构建（含 tsc --noEmit 类型检查）
npm run preview      # 本地预览构建产物

# 部署前端静态站（需已登录 tcb CLI）
tcb hosting deploy "app/dist" / -e test-d2gk9bnf2dc862288

# 后端（HTTP 云函数 + PostgreSQL；密钥不入库，全部生成到已 gitignore 的文件里）
node tools/backend.mjs secret     # 生成会话签名密钥 → .secrets/backend.env
node tools/backend.mjs apikey     # 生成数据库 API Key（service_role）→ 同一个文件
node tools/backend.mjs migrate    # 建表 drill_accounts / drill_progress（幂等）
node tools/backend.mjs deploy     # 生成 cloudbaserc.local.json（含两个密钥）并部署 api 函数
node tools/backend.mjs smoke      # 线上冒烟：健康 → 注册/登录 → 进度读写（真实 HTTP）
node tools/backend.mjs status     # 看函数与静态托管状态
```

- 线上地址：`https://test-d2gk9bnf2dc862288-1304936445.tcloudbaseapp.com`
- 后端入口：`https://test-d2gk9bnf2dc862288.service.tcloudbase.com/api`（前端只认这一个 base）
- 环境 ID：`test-d2gk9bnf2dc862288`（地域 ap-shanghai，体验版有效期至 2027-03-22）

---

## 1. 背景与定位

**一句话定位**：一个面向 AI 从业者的"每天 10 题"**工程知识**巩固系统——题库按知识域分类，答完即时给分层解析并**可溯源到一手资料**，长期沉淀个人掌握度画像。

**要解决的问题**

1. AI 知识更新太快，学了就忘，缺少高频小步的巩固机制。
2. 现有 AI 题库内容偏"面试八股"，与真实工程判断脱节。
3. 网上的题质量参差，答案错、无出处、不讲为什么。
4. 学完之后不知道自己的短板到底在哪一块。

**目标用户**

| 优先级 | 人群 | 核心诉求 |
| --- | --- | --- |
| P0 | 在职 AI 应用开发者 / 算法工程师 | 持续巩固、查漏补缺、验证工程判断 |
| P0 | AI 方向的学生、转行者 | 有体系的入门检验 |
| P1 | AI 产品经理 | 概念对齐、术语理解 |
| P2 | 企业团队内训 | 内部知识考核（后期 B 端） |

**核心价值**

1. **低门槛坚持**：10 题约 3–5 分钟，当天可完成。
2. **可信**：每题有出处（文档/论文链接 + 原文片段），不编题。
3. **贴近实战**：题面是工程场景判断，不是背诵式问答。
4. **有反馈**：错题知道错在哪、薄弱点在哪、下一步学什么。

**MVP 非目标（明确不做）**

- **不做面试题库 / 求职突击**（该赛道已被占位，见第 2 章）
- 课程售卖、直播、社区发帖讨论
- 组队 PK、排行榜、好友系统
- 原生 App（用 PWA 代替，见第 10 章）
- 付费体系

---

## 2. 竞品与差异化

> 完整调研见 `docs/research/竞品调研报告.md` 与 `docs/research/竞品调研-中文搜索.md`，本章只保留结论。

### 2.1 竞品地图

| 类别 | 代表产品 | 与我们的关系 |
| --- | --- | --- |
| A 直接同质：AI/大模型题库 | 面试鸭、吴师兄大模型、小林coding、牛客、AgentGuide | 最强竞争，但定位偏求职 |
| B 通用答题/考试平台 | 考试宝、问卷星、题答答、考试云、超星学习通、雨课堂、粉笔 | 功能可复制，内容不聚焦 AI 工程 |
| C AI 记忆/学习 App | Anki、RemNote、Brainscape、Quizlet、Knowt、StudyFetch、扇贝、百词斩、Duolingo | 交互与留存范式可直接借鉴 |
| D AI 出题工具（教师向） | QuestionWell、Conker、MagicSchool、Diffit、Yippity、Quizgecko、Kahoot/Quizizz | 出题技术同源，场景不同 |
| E 知识来源/免费替代品 | **Gemini Notebook（原 NotebookLM）**、ChatGPT 学习模式、OpenAI Academy、Anthropic Claude Academy、Hugging Face Learn、阿里云 ACP 认证 | 既是替代品，也是题库来源 |
| F 开源同构项目 | liyupi/yu-ai-learn、ai-knowledge-learning-miniapp、Smart-Quiz-Tutor、题喵喵、MianAI | 可直接参考实现 |

### 2.2 关键竞品对比

| 竞品 | 定位 | 它有什么 | 它缺什么 |
| --- | --- | --- | --- |
| **面试鸭**（mianshiya.com） | 程序员面试刷题，10000+ 题 | 专门的 AI 大模型面试题库（RAG/Agent/Prompt/LangChain/向量库/微调）、深度解析、Web+小程序+IDE 插件多端 | 面向求职，题目是面试问答式八股；无每日巩固机制、无长期掌握度画像 |
| **吴师兄大模型** | 大模型面试题库 + 训练营 | 851 题按真实面试链路重排，含项目面试题 | 同上 |
| **考试宝**（kaoshibao.com） | 通用在线考试刷题平台 | AI 出题、**AI 一键导题**（纸质题转在线题库）、错题本、组卷 | 内容是用户自己导入的，无权威 AI 知识题库 |
| **Gemini Notebook（原 NotebookLM）** | Google 的 AI 研究助手 | 从你的资料**生成测验和闪卡**，免费 | 无结构化题库、无分类体系、无跨会话长期掌握度、无质量审核 |
| **liyupi/yu-ai-learn** | 开源 AI 闯关学习小程序 | 主题 AI 出题、闯关答题与即时讲解、RAG 私有知识库出题、通关复盘 | 无题库分类体系、无每日推荐、无长期画像 |

### 2.3 市场空白

| 空白 | 说明 |
| --- | --- |
| **定位空白** | 现有中文产品全是"求职/考证"导向，**没有"在职工程师日常知识巩固"这一定位** |
| **形态空白** | 没有产品做"每天 10 题 + 跨会话长期掌握度画像"（Duolingo 范式 × AI 工程知识） |
| **内容空白** | 通用平台出题能力很强，但没有权威、可溯源的 AI 工程知识题库 |
| **解析空白** | 光说"解析详细"不构成差异（面试鸭也强调深度解析），**"可溯源到一手资料 + 干扰项错因"才是** |
| **付费已验证** | 阿里云 ACP 大模型认证的第三方题库大量存在（有 817 题带答案的版本），说明有人愿意为 AI 知识考核付费 |

### 2.4 我们的差异化主张（三条，必须全部做到）

1. **题库可溯源**：每道题都能点回一手资料（官方文档 / 论文 / 规范）的原文片段。
2. **工程判断题优先**：题目以"场景下的工程判断"为主，而非概念背诵。
   例：*"线上 RAG 召回率指标正常但答案质量下降，你应该先查哪一层？"*
3. **长期掌握度画像**：跨会话记录每个知识域的掌握度，驱动每日推荐。

**量化要求**：每日 10 题中，至少 3 题为工程判断 / 场景题。

**对抗免费替代品的唯一护城河**：每日任务感 + streak + 长期记录——这是 Gemini Notebook 这类"随手可用"工具不具备的。

---

## 3. MVP 成功指标

| 指标 | 目标 |
| --- | --- |
| 题库规模 | ≥ 500 题，覆盖 ≥ 8 个分类 |
| 次日留存 | ≥ 35% |
| 7 日留存 | ≥ 15% |
| 每日 10 题完成率 | ≥ 50% |
| 连续打卡 7 天用户占比 | ≥ 20% |
| 错题 7 天后二次正确率 | ≥ 60% |
| 用户"题目报错"率 | < 2% |
| 工程判断题占比 | ≥ 30% |
| **月度云成本** | **¥0（可选方案最高 ¥19.9/月）** |

---

## 4. 功能清单

### 4.1 原始需求（确认为 P0）

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| F1 | 题库分类 | 多级分类 + 多标签，分类体系见第 5 章 |
| F2 | 每日默认推荐 10 题 | 每天自动生成一份 10 题任务，规则见第 8 章 |

### 4.2 A. 答题核心体验

| 编号 | 功能 | 优先级 | 进 MVP |
| --- | --- | --- | --- |
| A1 | 题型：单选、多选、判断 | P0 | **是**（单选 71 + 判断 60；**多选题仍无**，题库里没有正确选项 > 1 的题） |
| A2 | 题型：工程场景判断题（题干为场景，选项为处置方案） | P0 | 是 |
| A3 | 题型：填空、简答（AI 判分 + 评分要点） | P1 | **是（v0.11，T-019）**：简答 180 道；批阅走「模型（自带 key）+ 本地要点覆盖率」双路，失败自动降级；填空未做 |
| A4 | 题型：代码题 | P2 | 否 |
| A5 | 即时反馈：选完立刻判对错并给出解析 | P0 | 是 |
| A6 | **分层解析**：正确答案为什么对 / **每个干扰项为什么错** / 延伸知识点 / **参考出处（原文片段）** | P0 | 是（v0.6 补齐原文片段渲染） |
| A7 | 答题模式：每日推荐、分类专项、错题重做 | P0 | 是（分类专项练习页 v0.9 补齐，见 `pages/Categories.tsx`） |
| A8 | 答题模式：随机模考、收藏练习 | P1 | 部分（收藏练习 v0.9 已通；**随机模考未做**） |
| A9 | 断点续答、进度保存 | P0 | 是（v0.9：答完即记进度，重进回到中断的那一题） |
| A10 | 限时挑战 / 连对连胜 | P2 | 否 |
| A11 | **离线可答**（无网络也能刷当天题目） | P0 | 是（由 PWA 天然支持，见 10.4） |

### 4.3 B. 复习与记忆

| 编号 | 功能 | 优先级 | 进 MVP |
| --- | --- | --- | --- |
| B1 | 错题本（答错自动收录） | P0 | 是 |
| B2 | 间隔重复复习 | P1 | 是（**v0.11 升级为艾宾浩斯**：间隔 1/2/4/7/15/30/60 天 × 熟练度倍率，按保持率 R = e^(-Δt/S) 排序，见第 8 章） |
| B3 | 收藏题目 + 个人笔记 | P1 | 部分（收藏 v0.9 打通 UI：答题页可收藏、复习页可练；个人笔记仍未做） |
| B4 | 题目反馈按钮（答案有误/表述不清/有争议） | P0 | 是（v0.6 补齐，存本地 `aq.reports.v1`） |
| B5 | **每日打卡与连续天数 streak** | **P0**（v0.2 从 P1 提升） | 是 |
| B6 | **进度导出/导入（换手机不丢数据）** | P0 | 是（明文 base64 + v0.9 口令加密，见 10.8） |
| B7 | **每题熟练度自评（已掌握 / 模糊 / 未掌握）** | P0 | **是（v0.11，T-018）**：用户自评优先于自动推断；缺省视为「未掌握」 |
| B8 | **查看答案即判为未掌握** | P0 | **是（v0.11，T-019）**：显式确认后记 `viewedAnswer` + `isCorrect: false`，熟练度置「未掌握」 |

> B5 提升理由：streak 是对抗"随手可用"免费替代品的核心护城河（见 2.4）。
> B6 新增理由：进度数据只存在手机本地，必须有导出手段兜底。

### 4.4 C. 数据与能力画像

| 编号 | 功能 | 优先级 | 进 MVP |
| --- | --- | --- | --- |
| C1 | 个人主页：累计答题数、正确率、连续天数 | P0 | 是 |
| C2 | **知识域掌握度图（按分类统计正确率）** | **P0**（v0.2 从 P1 提升） | 是（简版） |
| C3 | 薄弱点诊断与学习建议 | P1 | 否 |
| C4 | 正确率趋势曲线 | P2 | 否 |

> C2 提升理由："长期掌握度画像"是第 2.4 节明确的差异化主张之一，不能推迟。

### 4.5 D. 题库与内容生产（后台）

| 编号 | 功能 | 优先级 | 进 MVP |
| --- | --- | --- | --- |
| D1 | 题目增删改查、批量导入（JSON/CSV/Excel）、导出 | P0 | 部分（v0.9：CLI 支持校验/草稿/合并，无图形界面） |
| D2 | 分类与标签管理（多级分类 + 多标签） | P0 | 部分（分类集中定义在 `CATEGORIES` 并由 `--check` 校验，无管理界面） |
| D3 | AI 出题流水线（从文档/论文生成草稿） | P0 | 是（v0.9：`tools/gen-questions.mjs --draft`，支持调模型或离线模板） |
| D4 | 审核发布状态机：draft → review → published → archived | P1 | 否（未实现） |
| D5 | 语义去重（相似题检测） | P1 | 否 |
| D6 | 题目质量统计（作答数、正确率、报错数） | P1 | 否（未实现） |
| D7 | **溯源字段：来源类型、URL、原文片段** | P0 | 是 |
| D8 | **题库编译脚本：源数据 → `questions.json`** | P0 | 是（v0.9：题库源数据即 `data/questions/*.json`，`--check` 校验、`--merge` 写入） |

### 4.6 E. 系统能力

| 编号 | 功能 | 优先级 | 进 MVP |
| --- | --- | --- | --- |
| E1 | 账号体系 | P1 | **是（v0.14 反转，T-026~T-030）**：注册登录 + 自签会话令牌；进度与错题按账号隔离；**后端已部署并冒烟通过（T-033）**，见 10.12 |
| E2 | 响应式 Web，手机可用（PWA） | P0 | 是 |
| E3 | 管理后台 | P1 | **否**（v0.3 定为本地脚本；v0.9 该脚本已就位：`tools/gen-questions.mjs`） |
| E4 | 埋点与基础数据看板 | P1 | 否（本地统计足够） |
| E5 | 云同步（跨设备接着答） | P1 | **是（v0.14，T-028/T-030）**：本地优先 + 后台同步，离线照常答题；冲突走乐观并发（409 合并重投） |
| E6 | 每日定时提醒 | P1 | **是（v0.14，T-031）**：导出 `.ics` 交给手机自带日历按天响铃，见 10.13 |

> v0.3 调整理由：去掉账号与线上后台后，整个系统变成纯静态站，**可以完全免费部署**，且省掉 2 周工作量。B 端内训需要时再加。
>
> **v0.14 反转（E1 / E5 / E6）**：需求变成「进度要按账号隔离、要能持久、要能换设备接着答」，纯本地 + 导出码不够用了，因此加了一个最小后端。**成本仍然是 ¥0**（CloudBase 体验版额度内），代价是系统从「零运维」变成「有后端要管」。详见 10.12。

### 4.7 F. 增长与商业化（本期不做，仅预留结构）

分享成绩卡、组队学习、排行榜、企业内训版、付费题库。

---

## 5. 题库分类体系

对标竞品覆盖度（面试鸭 AI 大模型题库、阿里云 ACP 大模型认证考点、AgentGuide 大纲）整理，MVP 先落地 8–9 类：

| 分类 | 覆盖考点 |
| --- | --- |
| 大模型基础 | Transformer、注意力机制、位置编码、推理与采样参数、上下文窗口 |
| 提示工程 | CoT、Few-shot、结构化输出、提示注入与防御 |
| RAG | 切分策略、向量检索、混合检索、重排序、上下文组装、RAG 评估 |
| Agent | 规划、工具调用、多智能体、记忆机制、MCP 协议 |
| 本体与知识图谱 | RDF、OWL、SPARQL、SKOS、图谱增强 RAG |
| 微调与对齐 | SFT、LoRA/QLoRA、RLHF、DPO、数据构造 |
| 评估与可观测 | 评测集设计、LLM-as-Judge、Tracing、线上指标 |
| 工程与部署 | 推理服务、成本控制、缓存、并发与限流 |
| 多模态 | VLM、文生图、语音交互（P1 再开） |

**标签**：分类之外用标签做横向维度，如 `检索质量`、`成本优化`、`线上事故`、`选型权衡`。

---

## 6. 题目来源

### 6.1 五类来源总览

| 类型 | 说明 | 冷启动速度 | 质量 | 版权风险 |
| --- | --- | --- | --- | --- |
| A 人工自编 | 按知识点大纲自己写 | 慢 | 最高 | 无 |
| B 公开数据集 | 现成考试/评测题库 | 最快 | 中 | 中（需逐个确认 license） |
| C 权威资料 + LLM 生成 | 从文档/论文抽取生成 | 快 | 高（有溯源） | 低（合理使用 + 改写） |
| D 社区/UGC | GitHub 题库、面试题、论坛 | 快 | 参差 | 高 |
| E 企业内部资料 | 内部文档生成内训题 | — | 高 | 无（自有） |

**竞品可作为"覆盖面基准"**：阿里云 ACP 大模型认证的考点大纲、面试鸭 AI 大模型题库的分类、AgentGuide（GitHub 9.9k star）的目录结构——**只抄知识点覆盖面，不抄题**。

### 6.2 B 类：公开数据集（用于快速冷启动）

| 数据集 | 语言 | 与 AI 知识贴合度 | 备注 |
| --- | --- | --- | --- |
| MMLU / MMLU-Pro | 英 | 中 | 57 学科通用知识，AI/CS 相关题占比不高 |
| C-Eval | 中 | 中高 | 中文，含计算机/AI 相关学科 |
| CMMLU | 中 | 中 | 中文多学科 |
| AGIEval | 中英 | 中 | 高考/法考/公务员类 |
| GPQA | 英 | 低 | 研究生级理科难题，难度偏高 |
| ARC / HellaSwag / BBH | 英 | 低 | 更偏推理评测，不是知识题 |
| IFEval / MT-Bench | 英 | — | 指令遵循/开放对话，不适合做选择题 |

⚠️ 注意事项：

1. **License 必须逐个确认**，多数评测集仅授权研究用途，商用需谨慎甚至不可用。
2. **数据污染/泄漏**：这些题已被大量模型训练过，作为"考人"尚可，作为"考模型"无意义。
3. 与"AI 工程实践知识"的定位偏差较大，建议**只做灵感与补充，不做主力**。

### 6.3 C 类：权威资料 + LLM 生成（推荐的题库主力）

- **通用/基础**：arXiv（cs.CL / cs.AI / cs.LG）、ICLR / NeurIPS / ACL / EMNLP 的 Survey
- **RAG**：LangChain / LlamaIndex 官方文档、向量库文档（Milvus / Weaviate / pgvector / Chroma）、Microsoft GraphRAG、RAGAS 评估框架
- **Agent**：Anthropic "Building Effective Agents"、OpenAI Agents/工具调用文档、MCP 规范、ReAct / Reflexion / Toolformer 原始论文
- **本体/知识图谱**：W3C RDF 1.1、OWL 2、SPARQL 1.1、SKOS、schema.org、Protégé 教程、GO/SNOMED 等公开本体
- **提示工程**：OpenAI / Anthropic 提示指南、Prompt Engineering Guide（DAIR.AI）
- **微调与训练**：Hugging Face 文档与课程、LoRA / QLoRA / RLHF / DPO 原始论文
- **评估**：HELM、LMSYS Chatbot Arena、OpenCompass、SuperCLUE 文档与榜单方法
- **行业趋势**：Stanford AI Index、State of AI Report

### 6.4 A 类：人工自编（质量最高）

- 先写**知识点大纲**（每个分类 20–40 个考点），再按考点出题，保证覆盖度。
- **最值钱的是工程判断题**：把线上事故、调参经验、选型权衡做成题。这类题外部拿不到，也是与面试鸭的核心区别。

### 6.5 D 类：社区/UGC

- 可用作灵感：GitHub 面试题库、Awesome-LLM、技术社区问答。
- 知乎/公众号/掘金/小红书内容：**只做选题灵感，不直接复制**。
- 工具支持（v0.12）：`node tools/extract-notes.mjs --repo <owner/repo> --ref main` 能把「标题即问题、正文即答案」的笔记仓库批量解析成**简答草稿**。
  - 已用 `wdndev/llm_interview_note` 跑通，抽出 203 道（其中工程与部署 33、Agent 9 等）。
  - **红线**：仓库没有 LICENSE 就等于「默认版权保留」。这类素材只能停在 `content/drafts/` 等人工处理，
    **必须先确认授权口径再入库**（`AGENTS.md` 第 8 节）。技术上抽得出来 ≠ 可以公开发布。
- P2 阶段可开放用户投稿，走审核队列。

### 6.6 E 类：企业内部资料（未来 B 端）

内部 wiki、规范文档、项目复盘 → RAG 生成内训考核题。这是后续最可能的商业化路径之一。

### 6.7 内容生产流水线

```
采集信源 → 正文解析切分 → 知识点打标（分类/标签）
   → LLM 生成（题干 + 选项 + 答案 + 分层解析 + 干扰项错因 + 溯源片段）
   → 自动质检（格式完整 / 答案唯一 / 选项互斥 / 相似题去重 / 事实核对）
   → 人工审核 → 编译为 questions.json → 随前端发布
   → 数据回流（作答数、正确率、报错数）→ 修订或淘汰
```

**质量红线（必须满足才允许发布）**

1. 每题必须有 source（来源类型 + URL + 原文引用片段）。
2. 单选题选项必须互斥、答案唯一，禁止"以上都对/都不对"。
3. 解析必须写清楚"为什么对"**和每个干扰项的错因**。
4. 主观题必须提供评分要点（rubrics）才能上线。
5. 报错率 > 5% 的题自动下线待复核。
6. 每日推送中工程判断题占比 ≥ 30%。

---

## 7. 数据模型

v0.3 起，**题库数据与用户数据分离**：题库在构建期固化为常量，用户数据只存在浏览器本地。

> ⚠️ v0.5 更正：当前实现中题库是手写的 TS 常量（`app/src/data/questions.ts`），**并没有 `questions.json` 编译产物**；下表描述的是目标形态。

### 7.1 题库（构建期常量；当前实现为 `app/src/data/questions.ts`）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | string | 如 `q_rag_0042` |
| type | enum | single / judge / scenario（**当前 TS 类型只有这三种**；多选由「正确选项数 > 1」自动判定，没有独立的 multi 值） |
| is_practice | bool | 是否工程判断题（用于统计 30% 占比） |
| stem | string | 题干 |
| options | array | `{ key, content, is_correct, wrong_reason }` |
| explanation | string | 正确答案为什么对 |
| extension | string | 延伸知识点 |
| difficulty | int | 1–5 |
| categories | string[] | 分类 |
| tags | string[] | 标签 |
| source | object | `{ type, title, url, snippet }` |

### 7.2 用户数据（浏览器本地，localStorage / IndexedDB）

| 表 | 关键字段 |
| --- | --- |
| answer_records | question_id, chosen, is_correct, duration_ms, mode(daily/practice/review), answered_at |
| review_states | question_id, stage(1/3/7/15/30), next_review_at, wrong_count |
| bookmarks / notes | question_id, content |
| daily_tasks | date, question_ids[], completed_count |
| profile | streak, last_active_date, total_answered, total_correct |
| reports_outbox | question_id, reason(wrong_answer/unclear/disputed/other), created_at（v0.6 已实现，存 `aq.reports.v1`） |

> v0.3 的模型里**不需要数据库**，这是当时能零成本部署的前提（见 10.1）。
>
> **v0.14 补充**：上表仍是**本机**的权威数据源（离线优先）。新增的云同步只是「按账号存一份整包副本」：
>
> | 服务端表 | 关键字段 | 说明 |
> | --- | --- | --- |
> | `drill_accounts` | uid(uuid PK), username, username_key(unique), pwd_salt, pwd_hash, created_at, last_login_at | 账号；`username_key` 是大小写归一后的唯一键；口令只存 scrypt 摘要 |
> | `drill_progress` | uid(PK, FK), payload(jsonb), revision(bigint), updated_at | 进度整包 = 上表的 JSON 形态；`revision` 用于乐观并发 |
>
> 两张表**不授权给 anon / authenticated**；云函数要带 API Key（`DRILL_API_KEY`，其 JWT 的 `role=service_role`）才能读写——不带就退化成 anon，一碰库就 `permission denied`。**题库不入库**，仍是仓库里的 JSON。

**题目 JSON 示例**

```json
{
  "id": "q_rag_0042",
  "type": "scenario",
  "is_practice": true,
  "stem": "线上 RAG 服务的召回率指标正常，但用户反馈答案质量明显下降。你最应该先排查哪一层？",
  "options": [
    { "key": "A", "content": "调高 LLM 温度参数", "is_correct": false, "wrong_reason": "温度只影响生成随机性，与答案质量下降的因果关系最弱。" },
    { "key": "B", "content": "检查重排序后的 top-k 文档与问题的相关性", "is_correct": true, "wrong_reason": null },
    { "key": "C", "content": "把 chunk_size 调大", "is_correct": false, "wrong_reason": "chunk 变大反而稀释语义，且不是排查动作而是修改动作。" },
    { "key": "D", "content": "更换 embedding 模型", "is_correct": false, "wrong_reason": "更换模型成本高、影响面大，不应作为第一排查动作。" }
  ],
  "explanation": "『召回率正常』只反映候选集里包含相关文档，不保证排序后进入上下文的是相关文档。最常见故障是重排序或截断环节把相关文档挤出了 top-k。",
  "extension": "排查顺序建议：召回率 → 重排序结果 → 上下文组装 → 提示词 → 生成参数。",
  "difficulty": 2,
  "categories": ["RAG"],
  "tags": ["线上事故", "排查方法"],
  "source": {
    "type": "doc",
    "title": "LlamaIndex - Reranking",
    "url": "https://docs.llamaindex.ai/...",
    "snippet": "Reranking improves retrieval quality by ..."
  }
}
```

---

## 8. 每日推荐算法（v0.11：艾宾浩斯遗忘曲线驱动）

每天首次进入时（本地时间）为当前用户生成 10 题，纯前端计算：**先算复习到期，再补新题**。

**调度模型（`app/src/lib/ebbinghaus.ts`）**

| 概念 | 实现 |
| --- | --- |
| 基准间隔 | `BASE_INTERVALS = [1, 2, 4, 7, 15, 30, 60]`（天）。用日粒度而不是 5 分钟 / 30 分钟，因为产品是「每天 10 题」的日频节奏 |
| 熟练度倍率 | 已掌握 ×2 / 模糊 ×1 / 未掌握 ×0.5，最短 1 天兜底（用户自评会直接改变复习节奏） |
| 保持率 | `R = e^(-Δt / S)`，S 取上次排定的间隔天数；**低于 0.7 就算该复习** |
| 排序 | 「忘得最狠优先」——保持率越低越先出现 |
| 答对 / 答错 | 答对：档位前进一档，间隔 = 该档间隔 × 倍率；答错：回到第 0 档（次日照面）并累计 `wrongCount` |

**每日配额（`app/src/lib/recommend.ts`）**

| 池子 | 配额 | 规则 |
| --- | --- | --- |
| 到期复习池 | 5 题 | `isDue()`（过期或保持率 < 0.7），按保持率升序 |
| 未掌握回炉池 | 补足到 5 | 熟练度为「模糊 / 未掌握」且做过的题；**显式标「未掌握」的题不受 14 天重复窗口限制**（自评比时间窗更能说明「这题我还不会」） |
| 新题池 | 3 题 | 未作答过的题 |
| 薄弱分类池 | 1 题 | 正确率 < 60% 且作答 ≥ 3 的分类 |
| 挑战池 | 1 题 | 难度 ≥ 4 |
| 兜底 | 补足 | 任一池不足顺延；最终**一定凑满 10 题** |

**约束**

- 最近 14 天内已答过的题不重复推荐（显式「未掌握」除外）。
- 当日 10 题中工程判断题 ≥ 3 题（`MIN_PRACTICE`）。
- 新用户：先做一次 10 题均匀摸底（各分类轮流抽样），之后按规则推荐。
- 推荐结果当天固定（`daily_tasks` + 日期做种子的确定性随机），跨设备/跨天不变化。

**演进方向（P1/P2）**

规则引擎 → 知识追踪模型（BKT / DKT）→ IRT 难度自适应。

## 9. 关键页面与流程

下表左列为设计意图，右列为**实际实现状态（v0.5 核实）**。

| # | 页面 / 流程 | 实现状态 |
| --- | --- | --- |
| 1 | **今日答题页**（默认落地页）：当日进度、连续打卡、开始按钮、历史战绩 | ✅ 已实现（进度是条形进度条，非「进度环」） |
| 2 | **答题中**：进度 3/10、题干、选项、下一题、提交后立即反馈 | ✅ 已实现（**没有「标记疑问」**）；v0.11 起按题型渲染：判断/单选给选项，**简答给输入框**，并带熟练度选择器 |
| 3 | **反馈层**：对错 → 正确答案 → 为什么对 → **每个干扰项错因** → 延伸 → **出处链接与原文片段** → 报错按钮 | ✅ 已实现（v0.6 补齐原文片段与报错按钮） |
| 4 | **结算页**：正确率、用时、今日错题、streak 动效 | ✅ 已实现（**无 streak +1 动效**） |
| 5 | **错题本**：按分类筛选、重做、掌握度标记 | ⚠️ 部分（**无分类筛选**；重做与收藏已做，掌握度标记仍无） |
| 6 | **分类浏览页**：8 个分类进入专项练习 | ✅ 已实现（v0.9 新增 `pages/Categories.tsx`） |
| 7 | **我的数据**：累计答题、正确率、streak、**掌握度图**、进度导出/导入 | ✅ 已实现 |
| 9 | **简答批阅设置**（自带 API key / 模型 / baseURL） | ✅ 已实现（v0.11 新增 `pages/AgentSettings.tsx`；key 只存本机，不进进度码） |
| 8 | **管理工具**（本地脚本）：题目编辑、批量导入、审核、编译 questions.json | ❌ **未实现** |

---

## 10. 技术架构与部署方案（已定：CloudBase 静态托管 + Web PWA）

> 本章 v0.3 新增、v0.4 按实测结果重写。**路线已定，不再比较备选方案**：**纯静态 Web PWA + 腾讯云 CloudBase 静态网站托管**。该链路已实际部署跑通，见 10.5。
>
> 一句话：**不做微信小程序，不做原生 App，不买服务器。**

### 10.1 架构原则：把"需要服务器的部分"消掉

| 传统做法 | 我们的做法 | 结果 |
| --- | --- | --- |
| 题库存数据库，接口查询 | 题库在构建期固化为前端常量（目标是编译成 `questions.json`），随前端发布走 CDN | **不需要数据库** |
| 答题记录存服务端 | 记录存浏览器 localStorage（数据量上来后换 IndexedDB） | **不需要后端** |
| 运行时调 LLM 判分/出题 | 出题与审核在**离线脚本**中完成，运行时零 LLM 调用 | **不需要托管密钥** |
| 线上管理后台 | 本地 Node 脚本 + JSON 文件 | **不需要管理端服务** |

**v0.3 的结论：整个系统是一个纯静态 PWA，零服务器、零数据库、零运维。** 这既是零成本的原因，也是能快速上线的原因。代价是**不支持多设备同步**（只用 10.8 的进度导出码兜底）。

> ⚠️ **v0.14 修正**：上表仍然成立（题库依旧静态、运行时依旧零 LLM、依旧没有管理后台），但「**不需要后端**」这一条**已被推翻**：账号与同步需要一个后端。新增的边界很窄——
>
> - 后端**只做**两件事：账号（注册/登录/改密）与进度整包同步。**不碰题库**，不做出题，不做统计。
> - 前端**离线优先**：所有读写先落本机 localStorage，后端只是「顺路同步一份」。断网时功能不受影响（只是不同步）。
> - 前端**不持有任何数据库凭据**：请求走 HTTP 云函数，数据库账号只在云函数环境里。
>
> 架构与接口细节见 **10.12**。

### 10.2 技术选型

| 层 | 选择 | 说明 |
| --- | --- | --- |
| 前端 | Vite + React + TypeScript | 构建产物是纯静态文件，已在跑 |
| 样式 | 原生 CSS（当前，体积小） | MVP 不引入 Tailwind / UI 框架，避免额外依赖 |
| PWA | `vite-plugin-pwa` | 生成 manifest + service worker，可加到主屏幕、可离线 |
| 本地存储 | localStorage 封装（当前）→ 数据量上来后换 IndexedDB | 答题记录、错题本、掌握度、每日任务 |
| 后端（v0.14） | CloudBase **事件云函数 + HTTP 访问服务** + **PostgreSQL**（`@cloudbase/node-sdk` 的 rdb → PostgREST） | 只承担账号与进度同步；云函数用 API Key 拿 service_role，凭据只在函数环境变量里 |
| 会话 | **自签 HMAC-SHA256 令牌**（`Authorization: Bearer`） | 平台不允许用户名密码自助注册，所以不走平台登录态 |
| 题库 | 构建期常量（源数据 `app/src/data/questions/*.json`，由 `index.ts` 组装） | 现有 **315 题**：原始 JSON 约 487 KB（≈1.55 KB/题），构建后 JS **417.75 kB（gzip 187 kB）**；按此推算 500 题约 660 KB（gzip ≈290 KB）。PWA 预缓存 15 项 / 598 KiB。⚠️ 题库已是主包体积的主要来源，再扩量时考虑按分类懒加载或把题目挪出主 chunk |
| 出题/审核工具 | 本地 Node 脚本 + LLM API | 只在开发机运行，不部署 |
| 托管 | 腾讯云 CloudBase 静态网站托管 | 国内直连，见 10.3 |

### 10.3 部署方案（已定）

| 项 | 内容 |
| --- | --- |
| 平台 | 腾讯云 CloudBase 静态网站托管（环境类型：体验版） |
| 环境 ID | `test-d2gk9bnf2dc862288` |
| 地域 / 有效期 | ap-shanghai / 至 2027-03-22 |
| 访问地址 | `https://test-d2gk9bnf2dc862288-1304936445.tcloudbaseapp.com` |
| 部署命令 | `tcb hosting deploy "app/dist" / -e test-d2gk9bnf2dc862288` |
| 当前成本 | **¥0** |

**为什么不用 Cloudflare Pages**：国内可达性不可控（`pages.dev` 可能被墙或很慢），且本项目开发机走代理，无法代你验证真实网络。

**为什么不做微信小程序**（已评估并否决）：小程序 request 域名必须 HTTPS **且已完成备案**，个人主体做教育/答题类目还容易卡资质；PWA 零门槛就能覆盖同样需求。调研结论见 `小程序调研.md`，小程序作为 P2 备选。

### 10.4 手机访问方式：PWA 添加到主屏幕

1. 浏览器打开 `https://test-d2gk9bnf2dc862288-1304936445.tcloudbaseapp.com`
2. 若出现"访问提示中间页"，点一次「确定访问」（原因见 10.6）
3. **iOS Safari**：分享 → 添加到主屏幕；**Android Chrome**：菜单 → 添加到主屏幕
4. 得到一个全屏图标，无地址栏，体验接近原生 App
   - ⚠️ **iOS 已知问题**：manifest 只提供 SVG 图标，iOS Safari 不支持，**可能无法生成正确图标**。修复方案见 0.6
5. Service Worker 已缓存页面与题库 → **地铁里没信号也能答题**（对齐功能 A11）

### 10.5 部署链路（已跑通）

| 步骤 | 状态 |
| --- | --- |
| 安装并登录 CloudBase CLI（`tcb` 3.8.4） | ✅ |
| 创建环境 `test-d2gk9bnf2dc862288` | ✅ |
| 前端构建 `npm run build`（含 `tsc --noEmit`） | ✅ 无报错 |
| 本地 `vite preview` + 真实浏览器全链路验证 | ✅ |
| 静态托管上传（**12 个应用文件**，状态 Online） | ✅ 与本地 `dist` 逐字节一致（2026-09-23 复核） |
| **手机 4G/5G 关代理实测** | ⏳ **待验证，见 10.7** |
| v0.6 之后的历次代码改动重新部署 | ✅ **已补齐（T-034，2026-09-23）**：重新构建上传，旧哈希产物已清理 |
| **后端函数 `api` 部署（v0.14）** | ✅ **已执行（T-033，2026-09-23）**：函数按 Event 类型部署 + `--path /api` 挂 HTTP 访问服务；全链路冒烟通过（健康 / 注册登录 / 进度读写） |

### 10.6 ⚠️ 默认域名的硬限制（已查证，必须知道）

腾讯云官方文档 `https://docs.cloudbase.net/service/alias` 明确：

1. 默认域名 `*.tcloudbaseapp.com` 浏览器访问会**先弹"访问提示中间页"**，需点「确定访问」才能进站。
2. 默认域名**仅建议用于开发测试**，官方明确不建议用于正式生产或分发给大规模用户，**访问量异常会被风控关停**。
3. 想要**去掉中间页，唯一合规途径是绑定自定义域名，而自定义域名必须完成 ICP 备案**（约 2–3 周）。
4. **实测（2026-09-22 复验）**：命令行直连 `/`、`/manifest.webmanifest`、`/assets/*` 均返回 **HTTP 200**，仅不存在的路径返回 404。早前记录的「所有路径返回 418」**已不成立**。是否仍会出现浏览器中间页，需以手机实测（10.7）为准。

**结论**：自己用 / 小范围试用完全可行；但**"国内直连 + 永久免费 + 无中间页"这个组合并不存在**，三者最多取二。

**演进路径（按需再做，不提前投入）**

| 阶段 | 做法 | 成本 / 周期 |
| --- | --- | --- |
| 现在：自用验证 | 默认域名 + 中间页，点一次即可 | ¥0 |
| 要正式给别人用 | 绑自定义域名 + ICP 备案 | 域名 ¥10–70/年，备案免费但需 2–3 周 |
| 不愿备案 | 换 Cloudflare Pages + 自定义域名（接受国内可达性风险） | ¥0–70/年 |

### 10.7 手机实测清单（请你执行，结果决定要不要走备案）

用**手机移动数据（4G/5G）、关闭 VPN / 代理 / Wi-Fi**，逐项确认：

1. 能否打开 `https://test-d2gk9bnf2dc862288-1304936445.tcloudbaseapp.com`，中间页能否点过？
2. 首页是否正常显示「今日 10 题」？
3. 答一题 → 提交 → 解析与出处链接能否正常展开？
4. 刷新页面 → 进度是否还在（验证本地存储）？
5. 添加到主屏幕 → 图标能否生成、打开是否全屏？
6. （v0.9 新增）退出后重进 → 是否回到中断的那一题？
7. （v0.9 新增）底部「分类」Tab → 能否进入某分类练习、答完后覆盖数有没有变？
8. （v0.9 新增）收藏一题 → 「复习」页「收藏」标签里能否看到并练习？

> ✅ 线上已在 2026-09-22 重新部署（v0.9 产物），可以直接按清单实测。

### 10.8 数据安全兜底：进度导出码

数据只存在手机本地，清缓存或换手机会丢。因此：

- 提供"**导出进度码**"：把答题记录 / 复习状态 / 画像压成一串文本
- 换设备时粘贴导入即可恢复
- 这是**零后端实现跨设备迁移**的土办法，MVP 阶段够用
- **（v0.9）口令加密导出**：可选填口令（≥6 位），走 PBKDF2(SHA-256, 15 万次) + AES-GCM 加密，
  产物以 `AQ1.` 开头；导入时自动识别并要求输入同一口令。口令不对或密文被改都会拒绝导入。
- 注意：明文导出码仍然是纯 base64，**含答题记录与个人画像**，对外分享请一律用加密导出。

### 10.9 已核实的免费额度（核实日期 2026-09-22）

| 服务 | 免费额度 | 用途 |
| --- | --- | --- |
| 腾讯云 CloudBase（体验版） | 静态托管存储 + CDN 流量免费额度 | **本项目托管（当前）** |
| 腾讯云 CloudBase（个人版） | 新用户 0 元体验 **6 个月**，之后 **¥19.9/月** | 体验版额度不够时升级 |
| Google AI Studio（Gemini） | 有免费额度 | **离线出题**，不占运行时 |
| 硅基流动 / 本地 Ollama | 有免费模型 / 完全免费 | 离线出题的替代选择 |

⚠️ 免费额度随时可能调整，上线前请重新核对。

### 10.10 成本结论

| 场景 | 成本 |
| --- | --- |
| 自己用（默认域名 + 中间页） | **¥0** |
| 自用但要无中间页 / 分发给他人 | 域名 ¥10–70/年 + 备案（免费，2–3 周） |
| 离线出题用的 LLM API | ¥0（用免费额度） |

### 10.11 落地顺序（v0.5 更新为真实状态）

1. ✅ 前端骨架 + 题库数据模型 + 每日推荐引擎 + 答题/解析/错题本/统计（已完成，但见 0.6 的缺口）
2. ✅ 部署到 CloudBase 静态托管（已完成，线上与本地一致）
3. ⏳ **手机实测**（见 10.7）——唯一卡住的一步
4. ✅ 补 iOS PNG 图标 + maskable（v0.9 已完成）
5. ✅ 补最小缺口：渲染 `snippet`、加报错按钮（v0.6 已完成）
6. ✅ 题库从 24 题扩到 **315 题**（RAG / Agent / 本体各 100 题），工程判断题 **99 道**（≥60 达标）；出题脚本 v0.9 已就绪，批量扩充见 T-020 / T-021 / T-022
7. ⬜ 按实测结果决定是否绑定自定义域名 / ICP 备案（用户已表态「自己用、不碰备案」，默认不做）
8. ✅ 加后端：账号 + 同步（T-026~T-030）——代码完成，**部署待执行**（见 10.12）
9. ✅ 每日提醒（T-031）：导出 `.ics` 交给系统日历（见 10.13）

---

### 10.12 后端：账号与同步（v0.14 新增；**已部署并冒烟通过**）

**背景：这是一次决策反转。** v0.3 明确「不做账号、不买服务器」（见 4.6 的调整理由）；v0.14 的需求变成「进度按账号隔离 + 数据持久化」，纯本地 + 导出码不够用了，于是加了一个最小后端。**反转的是决策，不是当初的判断**——「单设备自用 → 纯静态够用」当时是对的，只是需求变了。**2026-09-23（T-033）已在体验版环境部署并跑通全链路冒烟**。

#### 10.12.1 数据通路（前端不碰数据库）

```
浏览器 --HTTP--> 云函数 /api/* --(API Key: service_role)--> PostgREST --> PostgreSQL
                （唯一持有数据库凭据的地方）
```

| 环节 | 说明 |
| --- | --- |
| 入口 | `https://test-d2gk9bnf2dc862288.service.tcloudbase.com/api`。函数按 **Event** 类型部署，再用 `fn deploy --path /api` 挂上 **HTTP 访问服务**；事件形状正好是适配层的 gateway 分支 |
| 云函数 | `cloud/functions/api/`，Nodejs20.19，入口 `index.main`；适配三类事件形态（gateway / framework / direct），本地直调与线上网关都能跑 |
| 数据库 | CloudBase **PostgreSQL 模式**（`rdb({ database })` → `.from()`）。体验版**没有**文档型数据库，这是实测后的选型 |
| 授权 | `drill_accounts` / `drill_progress` **不给 anon / authenticated 任何权限**（`revoke all`），只有 `service_role` 与数据库内部角色能读写 |
| 凭据 | 云函数**必须**带 `DRILL_API_KEY`：`init({ env, accessKey })`。函数自身的 CAM 签名只会被判成 `anon`，一碰库就是 `permission denied for table`（T-033 踩坑，见 `AGENTS.md` 第 9 节） |

#### 10.12.2 接口

| 方法 | 路径 | 作用 |
| --- | --- | --- |
| GET | `/health` | 健康检查（冒烟用，不碰数据库） |
| POST | `/auth/register` | 注册（`DRILL_ALLOW_REGISTER=off` 时 403） |
| POST | `/auth/login` | 登录 → 令牌 + 用户信息 |
| GET | `/auth/me` | 用令牌换当前用户 |
| POST | `/auth/password` | 改密（旧令牌随之失效） |
| GET | `/progress` | 读本账号的进度整包（含 `revision`） |
| PUT | `/progress` | 写进度整包，带 `baseRevision` 做乐观并发；对不上返 **409 + 服务端最新值** |

#### 10.12.3 三条必须知道的约定

1. **口令与令牌都是自管**：口令存 scrypt(N=16384) 摘要；会话是**自签 HMAC-SHA256 令牌**（不是平台登录态）——因为平台明文说明「不能只靠用户名 + 密码注册」。
2. **同步是「整包 + 乐观并发」**，不是字段级合并：`lib/merge.ts` 负责把远端包合进本地（记录去重、复习状态取更新、当天任务取并集等）。两台设备同时离线改同一题，最终以 revision 大的一方为准。
3. **同步的内容里没有题库，也没有模型 key**：题库不进数据库；`aq.agent.v1`（批阅用的 API key）既不上云，也不进进度码。

#### 10.12.4 运维命令与部署前置

命令见 0.8。四个关键点：

- **密钥不入库**：`tools/backend.mjs secret`（会话密钥）与 `apikey`（数据库 API Key）都生成到 `.secrets/backend.env`，`deploy` 时写进已 gitignore 的 `cloudbaserc.local.json` 并注入函数环境变量。
- **冒烟要碰数据库**：`smoke` 走真实 HTTP 全链路（健康 → 注册/登录 → `/auth/me` → 进度读写），只打 `/health` 不碰库，正是上面那个 anon 坑能溜过去的空当。探针账号固定为 `drill_smoke`（口令存 `.secrets/`），不会每次冒烟都往账号表里堆新行。
- **Windows 上 `tcb` 要用 node 拉起**：`execFileSync` 直接调 `tcb.cmd` 会失败，`tools/backend.mjs` 已改走 `node <cli>/bin/tcb`。
- **部署前需用户确认**（harness 铁律：不要擅自部署）。

#### 10.12.5 代价与风险

| 项 | 说明 |
| --- | --- |
| 从「零运维」变成「有运维」 | 多了一处要部署、要看日志的地方；体验版额度用完即停（见 10.9） |
| 注册默认开放 | 自用场景建议注册完自己就把 `DRILL_ALLOW_REGISTER` 关掉 |
| 没有找回密码 | 自用可接受；真忘了要在云后台改口令摘要 |
| 冲突是「后写覆盖」 | 见 10.12.3 第 2 条 |

### 10.13 每日提醒：导出 .ics 交给系统日历（v0.14 新增）

**为什么不用 Web Push / 系统闹钟**：

- 网页**没有**设置系统闹钟的能力（浏览器里不存在这个 API）。
- Web Push 在 iOS 上要求「已添加到主屏幕 + 用户授权」，后台唤醒还不稳定；自用场景不值得为它引入推送服务。
- 原生日历事件没有这些问题：**导入一次、按天重复、由系统日历响铃**——效果就是一个真闹钟，且零后端、零推送、断网也响。

做法（`app/src/lib/reminder.ts` + `app/src/pages/ReminderCard.tsx`）：

| 环节 | 说明 |
| --- | --- |
| 设置 | 「我的」页里：开关 + 时间（`HH:mm`）+ 提醒文案里的题量；存本机 `aq.reminder.v1`（**不进同步、不进进度码**） |
| 生成 | 一个 `VEVENT`：`DTSTART;TZID=Asia/Shanghai`（下次触发时刻；今天的点已过就落到明天）+ `RRULE:FREQ=DAILY` + `VALARM`（`TRIGGER:PT0M`，到点响） |
| 时区 | 固定东八区并带上 `VTIMEZONE`，换时区不会把触发时刻解析歪 |
| 折行 | 按 RFC 5545 做 75 **字节**折行（中文摘要必须按字节算，否则部分日历客户端解析失败） |
| 导入 | 手机上点开下载的 `.ics` → 选择「添加到日历」 |

已知限制：只支持一个每日时刻，不支持工作日/周末区分；**改时间要重新导出，并手动删掉旧日程**，否则两个提醒会同时响。

---

## 11. 里程碑（4 周）

| 周 | 交付内容 |
| --- | --- |
| W1 | 前端骨架、题库数据模型、离线出题脚本、**首批 200 题编译进站**（含 ≥ 60 道工程判断题）、本地预览 → **骨架已完成；出题脚本 v0.9 补齐；题库 v0.11 扩到 315 题，工程判断题 99 道，W1 目标达成** |
| W2 | 答题流程、即时反馈与分层解析、本地存储（localStorage）、每日推荐规则引擎 |
| W3 | 错题本、间隔复习、个人数据页、streak、掌握度图、进度导出码 |
| W4 | PWA 化（manifest + 离线缓存）、部署到 CloudBase 静态托管、**手机 4G 实测**、按实测结果决定是否绑域名备案、题库扩到 500 题 → **PWA 与部署已完成；题库已到 315 题（离 500 还差一截）；手机实测仍待办**（v0.14 另加两项超出原计划：后端账号/同步、每日提醒） |

---

## 12. 风险与应对

| 风险 | 说明 | 应对 |
| --- | --- | --- |
| **Gemini Notebook 免费替代** | 能从资料生成测验，Google 出品且免费 | 靠"每日任务感 + streak + 长期掌握度画像"错位竞争，这两点它没有 |
| **面试鸭向下兼容** | 已有 10000+ 题、多端、品牌，随时可加"每日推荐" | 抢先占位"日常巩固"心智；内容做工程判断题，不与其八股题正面竞争 |
| **通用平台内容补齐** | 考试宝有 AI 导题能力，用户可自行导入 | 提供开箱即用的高质量题库 + 溯源，降低用户成本 |
| **出题技术无壁垒** | AI 出题已商品化 | 壁垒放在审核流程 + 溯源 + 数据回流淘汰坏题 |
| **CloudBase 默认域名有中间页 + 风控** | 官方仅建议用于开发测试，访问量异常会被关停 | 自用可接受；要正式分发则绑自定义域名 + ICP 备案（见 10.6） |
| **CloudBase 体验版额度/有效期** | 体验版额度与 2027-03-22 有效期有限 | 到期前评估：升级个人版（¥19.9/月）或迁移 |
| ~~iOS 主屏图标缺失~~ | — | **v0.9 已修复**：补 192/512 PNG + maskable + apple-touch-icon |
| **本地与线上版本漂移** | 每次改完不部署，线上就落后于本地，实测会测到错版本 | 改完即部署（v0.9 已建立流程，见 0.8 的命令） |
| **题库与文档再次脱节** | 315 题的题型配比与批阅逻辑此前只写在代码与计划里，接手的人会以为还是「24 题纯选择题」 | v0.11 已把题库现状（0.5）、批阅与熟练度（0.3 / 4.2 / 4.3）、推荐算法（第 8 章）全部写回本文档；后续每次加题/改题型都要同步 |
| **文档与代码脱节（v0.5 新增）** | 第 4 章曾把 13 项未实现功能标为「是」，会误导后续排期 | v0.5 已改为真实状态；后续每次改动需同步更新第 0 章 |
| **本地数据丢失** | 清缓存/换手机会丢进度 | ✅ v0.14 已加后端云同步（按账号）；进度导出码保留为兜底 |
| ~~无账号导致无法同步~~ | — | ✅ **v0.14 已解决并上线**：注册登录 + `/progress` 按账号隔离（T-033 部署，见 10.12） |
| ~~前端静态产物未更新~~ | — | ✅ **T-034 已解决（2026-09-23）**：重新构建上传，线上与本地 `dist` 逐字节一致 |
| **后端引入运维负担**（v0.14 新增） | 从零运维变成有后端：额度、风控、口令遗忘 | 体验版额度内免费（超额即停不扣费）；注册完可一键关闭注册；口令丢失属自用可接受 |
| **同步覆盖本地数据**（v0.14 新增） | 整包同步 + 乐观并发，理论上远端旧包可能盖掉本机新进度 | `lib/merge.ts` 按字段合并而不是整包替换，并对无变化的包跳过上传；合并规则有 9 个用例守着 |
| LLM 生成题目有幻觉/答案错 | — | 强制溯源 + 人工审核 + 报错率自动下线 |
| 版权问题 | 面试题/课程内容有版权 | 优先用官方文档/论文；社区内容只做灵感，自行改写 |
| 冷启动题量不足 | — | 公开数据集 + LLM 批量生成 + 自编核心考点 |
| 用户坚持不下来 | — | 10 题低门槛 + streak + 每日推送（P1） |
| 题目难度失衡 | — | 用真实正确率反向校准 difficulty 字段 |

---

## 13. 待确认问题

1. 目标用户是自己用、团队内部用，还是要做成对外产品？
   → **已决定：自己用**（2026-09-23），明确「不碰备案」。后端已按自用口径实现（默认域名 + 中间页可接受）
2. 需要排行榜/组队/企业内训这类多人功能吗？
3. 题型只做选择题，还是必须支持 AI 判分的主观题？
   → **已决定：要，v0.11 已实现（判断题 + 简答题 + Agent 批阅 + 本地兜底，见 4.2 / 4.3）**
4. 是否需要中英双语题库？
5. 技术栈有偏好吗（Vite+React / Next.js / 其他）？
6. 你手上是否已有资料或题目素材可以直接作为冷启动来源？
7. 是否接受「**先做 Web/PWA、不做小程序**」的 MVP 范围？
   → **已在 v0.4 决定：接受，走 PWA 路线（见 0.2）**
8. 工程判断题的素材来源：你自己有没有线上事故、选型经验的沉淀可以转成题？
9. **（v0.3 新增）能否接受「不做账号、数据只存本机 + 导出码」作为第一版？**
   → **已实现并通过验证（见 10.8）**；**v0.14 起改为要做账号**：本地优先仍是主路径，另加云端同步（见 10.12）
10. **（v0.3 新增，v0.4 更新）手机实测结果如何？** 见 10.7 清单。用户已表态「自己用、不碰备案」，所以默认接受默认域名的中间页；实测只用于确认功能是否正常。
11. **（v0.14 新增）后端什么时候部署？** → **已于 2026-09-23 部署（T-033）**，`smoke` 全链路通过。仍待用户侧补测：注册两个账号 → 进度互相看不见 → 换设备能拉回来（单账号的读写链路由 `smoke` 覆盖）。

---

## 附录 A：变更记录

### A.1 v0.13 → v0.14（本版）

本轮做的是**架构决策反转：加后端 + 账号 + 每日提醒**（T-026~T-031）。用户的需求很明确：① 注册登录，每天的刷题进度与错题按账号隔离；② 数据要持久化；③ 题库仍以 JSON 形式留在代码仓；④ 每天定时提醒，最好能像闹钟一样响。并明确「**自己用，不用碰备案相关的**」。

**为什么推翻 v0.3 的「纯静态、不做账号」**：v0.3 那一刻的判断依据是「零服务器 = 零成本 + 省 2 周」，前提是单设备自用。现在需求里出现了「进度要持久、要能换设备接着答」，纯本地 + 导出码已经不够用了，所以决意引入最小后端。**注意：这只是决策被新需求推翻，不是当初判断错了**——若以后要做成对外产品，10.6 的备案问题会立刻回来。

| 变更 | 说明 |
| --- | --- |
| **技术选型（T-026）** | 存储用 **CloudBase PostgreSQL 模式**（体验版没有文档型数据库，实测确认）。前端**不直连数据库**，也不持有任何数据库凭据：浏览器 → 云函数 → PostgREST，云函数内部用 service_role 读写；`drill_accounts` / `drill_progress` 两张表**刻意不授权给 anon / authenticated**（已核实只有 service_role 与数据库内部角色有权限） |
| **部署与踩坑（T-033）** | 函数按 **Event** 部署（`cloudbaserc.json` 里 `type: HTTP` 指 Web 云函数，形态不对），再用 `fn deploy --path /api` 挂 HTTP 访问服务。**踩坑**：云函数自身的 CAM 签名在网关里只是 `anon`，被 revoke 的表一律 `permission denied`——症状是 `/health` 200 但 `/auth/register` 500 `DB_ERROR`。修法是 `init({ accessKey: DRILL_API_KEY })`（key 的 JWT 带 `role=service_role`），并把 `smoke` 从「只打 /health」升级为真实全链路 |
| **后端骨架（T-026）** | `cloud/functions/api/`：`index.mjs` 适配三类事件形态（gateway / framework / direct）+ 后缀路由 + 结构化日志 + 入参校验 + `/health`；配套 `tools/backend.mjs`（secret/migrate/deploy/smoke/status）与 `schema.sql` |
| **账号体系（T-027）** | 平台**不允许「用户名 + 密码」自助注册**（官方原文：`you can not signup just by username and password`），所以注册登录完全在应用内闭环：scrypt(N=16384) 存口令摘要，**自签 HMAC-SHA256 会话令牌**。接口 `/auth/register` `/auth/login` `/auth/me` `/auth/password`；`DRILL_ALLOW_REGISTER=off` 可一键关注册 |
| **进度同步（T-028）** | `/progress` GET/PUT：整包进度按账号隔离；写入带 `baseRevision` 做乐观并发，对不上返 **409 并回带服务端最新值**，由前端合并后重投一次 |
| **前端接入（T-029）** | `lib/api.ts`（`ApiError` / 离线语义）、`lib/session.ts`（令牌本机存储）、`lib/account.ts`（编排 + 错误码翻译成人话）、`pages/LoginForm.tsx` / `AccountPanel.tsx` / `Account.tsx`，底部导航新增第 5 个 Tab「我的」 |
| **本地优先同步（T-030）** | 仍是**离线优先**：答题先写 localStorage，再 4 秒防抖后台上传；远端有更新则合并落地并重读界面。`lib/storage.ts` 收口出 `readAll` / `writeAll`，`lib/merge.ts` 定义合并规则 + 规范化比较（避免无意义上传）。**地铁里没信号照样答题**，联网后自动补同步 |
| **每日提醒（T-031）** | 网页设不了系统闹钟，Web Push 在 iOS 上也不可靠，所以做成**日历订阅**：`lib/reminder.ts` 生成 `.ics`（带 `VTIMEZONE` + `RRULE:FREQ=DAILY` + `VALARM` + 75 字节折行），用户在「我的」页导出后交给手机自带日历——到点由系统响铃，等于真闹钟，**零后端、零推送、断网也响** |
| **题库保持 JSON** | 按用户要求，题库**不进数据库**，仍然是 `app/src/data/questions/*.json`；同步的只是「用户进度整包」（records / reviews / profile / bookmarks / mastery / tasks），后端不认识题目 |
| **模型批阅 key 不上云** | 仍只存本机 `aq.agent.v1`，既不进同步整包，也不进进度导出码（这是刻意的：key 不该跟着数据到处跑） |
| **门禁同步扩容** | `tools/check-layers.mjs` 纳入 `cloud/` 的单文件预算（≤250 行）；`tools/test-related.mjs` 纳入云函数测试（`node --test "cloud/functions/api/test/*.test.mjs"`）；新增 `hooks/` 层（rank 2.5，只允许被 pages 依赖） |
| **测试与校验** | vitest **496 个用例**（17 文件）+ 云函数 **35 个**（`node --test`）+ 流水线 127 个全绿；`tsc --noEmit` 无错；`npm run build` 通过（bundle 433.04 kB / gzip 192.89 kB）；`check-layers` 通过 |
| **部署状态** | 后端与前端**都已上线**（T-033 / T-034，2026-09-23）：`smoke` 全链路通过，前端 12 个文件与本地 `dist` 逐字节一致。仍未做：① 手机 4G 实测；② 同步是整包覆盖 + 乐观并发，不是字段级 CRDT；③ 注册默认开放，自用建议注册完关掉 |

### A.2 v0.12 → v0.13

本轮是一次**线上功能体检（T-025）**：先用 Playwright 把部署好的站点从头到尾走一遍（首页 → 今日 10 题 → 单选/判断/简答作答 → 解析 → 查看答案 → 结果页 → 复习/分类/我的数据 → 导出进度码 → 刷新验证持久化），再回头修查出来的问题。

**结论：功能闭环是通的**，主要流程都能跑；查出的两个缺陷已修。

| 变更 | 说明 |
| --- | --- |
| **修复：本地评分阈值标定错误** | `HIT_COVERAGE` 原为 0.5，是按 `grading.test.ts` 里那个「近乎逐字抄写」的小夹具调出来的。换成题库 180 道真实简答题实测：拿**参考答案原文**去自评，只有 **21%** 能到 partial 线、**4%** 能到 correct 线 —— 标准答案自己都判不及格，用户写对也拿不到分。这是简答批阅（无 key 时的默认路径）实际上不可用 |
| **重新标定到 0.2** | 实测对照（180 道真题）：阈值 0.5 → 参考答案 4%/21%、跑题答案 0 分；0.25 → 57%/94%、跑题 1 分；**0.2 → 74%/97%、跑题 3 分**。取 0.2：标准答案基本都能过线，跑题答案依旧拿 0 分，区分度没有丢 |
| **新增题库级回归测试** | 不再只测那个玩具夹具，而是直接对全库简答题断言：「≥85% 的参考答案至少判 partial」+「跑题答案均分 <10」。以后谁再调阈值，先过这组 |
| **修复：「直接查看答案」误显示答对** | 简答题 `correctKeys` 为空，`choiceCorrect` 因此恒真，界面显示「✅ 答对了」并配绿色卡片 —— 而记录里写的是 `isCorrect: false`、熟练度置「未掌握」。**数据是对的，只有显示在撒谎。** 现在 `viewedAnswer` 单独判错，横幅改为中性的「👀 已查看答案」，卡片用琥珀色 |
| **顺带：去掉空的「正确答案：」** | 简答题没有选项，解析页 / 结果页 / 错题本三处都会渲染出一个空尾巴「正确答案：」。新增 `lib/question-kind.ts` 的 `correctAnswerLabel()` 统一处理，无选项时不再显示 |
| **线上已同步并清理** | 线上 12 个文件与本地 `dist` **逐字节一致**（sha256 比对全绿）；`tcb hosting deploy` 只增不删，顺手清掉了历史遗留的 5 个旧 JS/CSS（约 1.5 MB） |
| **测试与校验** | vitest **425 个用例通过**（+5）；`tsc --noEmit` 无错；`node tools/check-layers.mjs` 通过；`gen-questions --check` 通过（315 题） |
| **仍未解决** | ① 手机 4G 实测仍未做（唯一的 P0）；② 字面匹配的固有限制仍在：要点写「OWA」而用户写「开放世界假设」会判漏，根治要配模型 key 或引入同义词表；③ 203 道社区笔记草稿仍待版权口径 |

### A.3 v0.11 → v0.12

本轮做的是**笔记型仓库抽题（T-023）**：把「标题即问题、正文即答案」的社区面试笔记批量变成简答草稿。**边界很清楚——只产出草稿，不入库**，因为源仓库没有 LICENSE。

| 变更 | 说明 |
| --- | --- |
| **抽题模块（T-023）** | 新增 `tools/pipeline/notes.mjs`（纯函数：目录→分类映射、按标题层级切分、问句判定、清洗、要点抽取、去重）。与业务解耦，不 import `gen-questions.mjs` |
| **抽题 CLI（T-023）** | 新增 `tools/extract-notes.mjs`：`--repo`（走 `raw.githubusercontent.com` 按文件拉取）/ `--local`（离线只解析）/ `--cache-only`（只拉仓库树做调研）；并发 6、单文件失败只记 error 不中断、记录 commit sha 保证可复现 |
| **别整仓 clone** | 这类仓库 Markdown 正文只有 ~1 MB，图片却上百 MB，直连 clone 每秒几十 KB。按文件走 raw 接口约 1 秒/文件 |
| **抽取结果** | 203 道简答草稿，6 个分类文件（微调与对齐 84 / 大模型基础 54 / 工程与部署 33 / 评估与可观测 13 / 提示工程 10 / Agent 9），203/203 通过 `question-schema.mjs` 校验 |
| **修掉的三处质量缺陷** | ① 答案与 `keyPoints` 残留 `**` 加粗、反引号、`&#x20;` 实体（应用是纯文本渲染，会直接露给用户）② `tidyCjkSpaces` 用 `\s` 连段落换行一起吃掉，中文段落被粘成一整段 ③ `QA:` 前缀正则漏写 `A`，作者的 `QA:` 标注没被剥掉 |
| **段落排版** | `cleanAnswer` 现在会合并段落内的软换行（中文之间不补空格、西文补一个），保留空行分段与列表换行；`styles.css` 的 `.exp p` 加 `white-space: pre-wrap`，让多段参考答案在页面上正常换行 |
| **版权口径（新增铁律 6）** | `AGENTS.md` 新增第 8 节：许可明确（论文/官方文档/规范）可直接入库；**许可不明（无 LICENSE 的社区笔记）必须先问用户**，草稿一律带 `待复核` 标签与 `community` 来源 |
| **测试** | 新增 `tools/pipeline/notes.test.mjs` 34 个用例（`isQuestionTitle` / `parseSections` / `cleanAnswer` / `pickKeyPoints` / `classifyPath` / 去重 / `toDraftQuestion` 等）；流水线用例总数 93 → **127**，全绿 |
| **未做** | 203 道草稿**没有并入题库**，等用户决定使用方式；题库仍是 315 题 |

### A.4 v0.10 → v0.11

本轮一口气做完四件事：**加题型（判断 / 简答）+ Agent 批阅 + 艾宾浩斯推荐 + 题库扩到 315 题**。此前文档只写「题库 24 题、题型只有选择」，这一版把真实状态全部写回第 0 章与第 4 / 8 / 9 章。

| 变更 | 说明 |
| --- | --- |
| **判断题 / 简答题型（T-018）** | 题型契约扩展：判断题恰好 2 个选项（A 正确 / B 错误，错项必须写 `wrongReason`）；简答题 `options: []` + 必填 `referenceAnswer` + 可选 `keyPoints`；`tools/question-schema.mjs` 与 `all.test.ts` 同步收紧 |
| **熟练度三档（T-018）** | `lib/mastery.ts`：已掌握 / 模糊 / 未掌握。合并规则为「用户显式自评 > 自动推断；同为自动则取更晚」，缺省 = 未掌握 |
| **艾宾浩斯复习调度（T-018）** | 新增 `lib/ebbinghaus.ts`（间隔 1/2/4/7/15/30/60 天 × 熟练度倍率，保持率 `R = e^(-Δt/S)`，到期阈值 0.7）；删除旧的固定间隔 `lib/review.ts`；`lib/recommend.ts` 改为「忘得最狠优先」，配额 复习 5 + 新题 3 + 薄弱 1 + 挑战 1 |
| **简答批阅：双路（T-019）** | `lib/grading.ts` 本地要点覆盖率评分（中文按相邻二字 token、英文按单词做模糊匹配，命中率 ≥0.5 记命中要点，≥80 判 correct / ≥40 判 partial）；`lib/agent.ts` 用**用户自带 key** 直连 OpenAI 兼容 `/chat/completions`，未配置或调用失败时自动降级本地评分（`degraded: true`） |
| **自带 key 的存储边界** | 配置存 `localStorage: aq.agent.v1`，**不参与进度码导出**（避免 key 被导出码带出去） |
| **查看答案即未掌握（T-019）** | 显式确认后记 `viewedAnswer: true` + `isCorrect: false`，熟练度一律置「未掌握」 |
| **作答 UI 拆分（T-019）** | 新增 `pages/ShortAnswerBox.tsx`（简答输入）、`QuestionBody.tsx`（题干与选项）、`MasteryPicker.tsx`（熟练度）、`AgentSettings.tsx`（批阅配置）、`NavBar.tsx`；`pages/Quiz.tsx` 重写为 200 行 |
| **题库：RAG 100 题（T-020）** | 20 单选 + 20 判断 + 60 简答；覆盖切分 / 混合检索 / 重排序 / 上下文压缩 / 增量索引 / 解析 / 缓存 / 路由 / 合规 / 排障 / 评估等。两道历史 `scenario` 题归正为 `single`，使「20 单选」口径成立 |
| **题库：Agent 100 题（T-021）** | 同上配比；覆盖 ReAct 循环、工具设计与选择、参数校验、上下文治理、错误恢复、记忆分层、多智能体、沙箱安全、幂等、降级、可观测、回归测试、验收与长期维护 |
| **题库：本体与知识图谱 100 题（T-022）** | 同上配比；覆盖 RDF / RDFS / OWL / SHACL / SPARQL、OWA 与 UNA、IRI 设计、知识抽取与融合、实体链接与对齐、图谱嵌入与链接预测、GraphRAG、KGQA、质量评估与治理 |
| **工程判断题（T-002）** | 按标签把排障 / 运维 / 安全 / 成本 / 选型 / 监控 / 合规等主题的题标为 `isPractice`，共 **99 道**；`all.test.ts` 的不变量同步收紧为「≥60 道且跨 ≥3 个分类」 |
| **题库合并修复** | `tools/gen-questions.mjs --merge` 取号会填补被删除的空号，旧实现只做追加会把文件内顺序打乱，违反「id 成块升序」不变量；现在合并后统一按 id 升序落盘，提示信息也从 `EXPECTED_IDS` 更正为 `EXPECTED_FILES` 计数 |
| **题库不变量测试重写** | `all.test.ts` 不再逐条列出几百个 id（没人会看），改为锁定「每个文件的题量 + 各文件 id 成块升序」；字段完整性测试改为按题型分支：简答题校验「无选项 + 有参考答案与要点」，判断题校验「恰好 2 个选项」 |
| **测试与校验** | vitest 全量 **420 个用例通过**（11 个文件，含新增 ebbinghaus 16 / mastery 9 / grading 9 / agent 13 / recommend 7）；`node tools/gen-questions.mjs --check` 通过（题库 315 题）；`tsc --noEmit` 无错 |
| **已部署** | 后端由 T-033 上线，前端由 T-034 重新构建上传；线上 12 个文件与本地 `dist` 逐字节一致，旧哈希产物已清理 |

### A.5 v0.9 → v0.10

本轮做的是**题目获取流水线（T-013）**：把「从权威开源项目拿到可引用的素材 → 规范化 → 切分 → 出草稿」做成一条可重复运行、与业务解耦、全程有结构化日志的流水线。目标是让扩题库不再依赖手抄素材。

| 变更 | 说明 |
| --- | --- |
| **权威源注册表** | 新增 `tools/pipeline/sources.mjs`：6 个源（OpenAI Cookbook、DAIR.AI Prompt Engineering Guide、微软 generative-ai-for-beginners、HuggingFace Transformers 文档、OWASP LLM Top 10、arXiv cs.CL/cs.AI）。每个源都带 `license` / `homepage` / `authority`（为什么可信），抓取规则用 `include` / `exclude` / `minBytes` 声明 |
| **四个 stage** | `fetch`（GitHub 树 API 选文件后抓 raw；arXiv 解 Atom）→ `normalize`（去噪 → CorpusDoc）→ `chunk`（切块 + 跨源去重）→ `draft`（`extract` 离线草稿 / `llm` 调模型）。stage 之间只通过 `content/` 下的 JSON 契约通信 |
| **与业务解耦** | `tools/pipeline/*` 不认识「题库 / 分类白名单」这些概念，只产出素材与草稿；**唯一业务写入口仍是 `tools/gen-questions.mjs --merge`**。依赖方向：pipeline 与 gen-questions 互不 import，都只依赖 `tools/question-schema.mjs` |
| **逐字可引用的出处** | 每个 chunk 同时带 `text`（规范化，供出题阅读）与 `verbatim`（原文切片，供题目 `source.snippet`）。normalize 的清洗规则只允许「删除」类变换，并用 `checkBlocksVerbatim()` 校验块原文是素材的连续子串；不满足就记 `corpus.nonverbatim` 告警 |
| **结构化日志** | 新增 `tools/pipeline/logger.mjs`：每条 JSONL 带 `ts/level/runId/stage/msg/ms`；`timer()` 记耗时，`count()` 记计数（`docs.fetched` / `docs.skipped` / `corpus.docs` / `corpus.nonverbatim` / `chunks.total` / `drafts.total`）；自动脱敏（api key / token / cookie → `***`）。日志落 `content/logs/<runId>.jsonl` |
| **运行报告** | 每次运行写 `content/reports/<runId>.json` 与 `latest.json`：各 stage 的处理量、warn/error 明细、下一步该跑什么命令 |
| **带重试的 HTTP 客户端** | 新增 `tools/pipeline/http.mjs`：4xx（除 429）不重试，429/5xx 指数退避；实现可注入，故编排逻辑离线可测 |
| **单源失败不中断** | 一个源抓取失败只记 error 并继续下一个源；整轮有 error 时进程退出码为 1，方便 CI / 定时任务发现 |
| **抓取原则** | 只用 `blob` 地址做出处（人可读、可核对），抓正文走 `raw`；arXiv 只取摘要，并标注 `textKind: abstract`（当作「作者观点」而非定论） |
| **测试** | 流水线新增 4 个测试文件（`logger` 13 / `arxiv` 7 / `sources` 11 / `stages` 5 个用例），并把 `normalize` / `chunk` 的用例改为 text/verbatim 双轨语义；全套 `node --test "tools/pipeline/*.test.mjs"` 共 **93 个用例通过** |
| **产物不入库** | `.gitignore` 补 `content/raw`、`corpus`、`chunks`、`logs`、`reports`（都可由 `content/sources/` 或权威源重建） |
| **实测证据** | `node tools/pipeline/run.mjs --all --limit 2 --source owasp-llm-top10`：`docs.fetched=2`、`chunks.total=12`、`drafts.total=1`、`corpus.nonverbatim=0`，warn/error 均为 0；草稿出处（url / title / snippet）与原材料逐字一致 |

### A.6 v0.8 → v0.9

本轮把「0.3 进度表里剩下的待办」全部做掉（除题库扩容，按用户要求暂缓），并把题库改为可流水线生产的 JSON 源数据。

| 变更 | 说明 |
| --- | --- |
| **补 iOS/Android 图标** | 新增 `tools/gen-icons.mjs`（零依赖：自带 SDF 光栅化 + PNG 编码）生成 180/192/512 PNG 与 maskable；`index.html` 补 `apple-touch-icon` 与全屏 meta；manifest 改 3 张 PNG |
| **分类专项练习页（A7）** | 新增 `lib/categories.ts`（进度统计 + 未练→错题→已对 的出题顺序）与 `pages/Categories.tsx`；底部导航加「分类」Tab |
| **断点续答（A9）** | 新增 `lib/task.ts`（firstPendingIndex / pendingCount / markCompleted）；答完一题立即写入当天任务，重进从第一道没做的题继续 |
| **收藏打通（B3 上半）** | 答题页「☆ 收藏」，复习页改成「错题 / 收藏」双标签，可练习收藏题；顺手把 Quiz 的解析区抽成 `pages/QuizExplanation.tsx`（Quiz.tsx 205 → 158 行） |
| **进度码口令加密** | 新增 `lib/crypto.ts`：PBKDF2(SHA-256, 15 万次) + AES-GCM，`AQ1.` 前缀区分明文；统计页可加密导出、自动识别并解密导入 |
| **题库源数据改 JSON** | `data/questions/*.ts` → `data/questions/*.json`（内容逐题迁移，24 题 id 顺序不变）；`index.ts` 统一断言为 `Question[]` |
| **离线出题流水线（D3/D8）** | 新增 `tools/gen-questions.mjs`：`--check` 校验不变量、`--draft`（调模型或 `--offline` 模板）出草稿、`--merge` 校验后合并去重；素材放 `content/sources/`，草稿进 `content/drafts/`（gitignore） |
| **接入 CI** | 新增 `.github/workflows/ci.yml`：题库校验 → 分层与体积 → 构建（含 tsc）→ 全量测试；首次运行通过 |
| **门禁补强** | `pre-commit` 增加题库校验；`tools/test-related.mjs` 现在能识别 `.json` 依赖（改题库会选中 `all.test.ts`） |
| **测试从 50 → 81 个** | 新增 `categories`(11) / `task`(10) / `crypto`(10) 三个纯逻辑测试文件 |
| **仓库整理** | 根目录 4 个调研文件移入 `docs/research/`；主文档版本与引用路径同步 |
| 本轮提交 | 拆成 9 个提交（每 800 行内：143 / 655 / 290 / 366 / 374 / 45 / 249 / 162 / 218 …），全部带任务号 |

### A.7 v0.7 → v0.8

本轮为**工程化建设**：给仓库建立开发约束（agent harness），并把最臃肿的文件按分层规则拆开。

| 变更 | 说明 |
| --- | --- |
| **新增 `AGENTS.md`** | 仓库开发约束：五条铁律（分层、计划、行数、测试范围、完成即提交）、分层地图、改什么读什么、禁止事项、目录地图 |
| **新增 `tools/plan.mjs`** | 维护 `docs/PLAN.md`：add/start/done/show；只保留最近 5 条已完成，更早的自动归档到 `docs/plan-archive/<年月>.md` |
| **新增 `tools/check-commit-size.mjs`** | 单次提交改动不超过 800 行（新增 + 删除；lockfile、二进制、纯重命名不计入），由 `.githooks/pre-commit` 自动拦截 |
| **新增 `tools/check-layers.mjs`** | 依赖只能向下（types → data → lib → pages → entry）；单文件超 200 行警告、超 250 行拦截 |
| **新增 `tools/test-related.mjs`** | 依据改动用 import 图求传递依赖闭包，只跑受影响的测试文件，不跑全量 |
| **新增 `tools/finish-task.mjs`** | 收尾一条命令：行数校验 → 分层校验 → 相关测试 → 更新计划 → 提交（自动带任务号） |
| **引入 vitest + 50 个用例** | 覆盖 `lib/review`、`lib/stats`、`lib/storage`（含进度码导出导入），以及题库不变量（锁定 24 题 id 顺序、错误选项必须有错因、来源 URL 必须 https 等） |
| **题库按分类拆分** | `data/questions.ts`（574 行）拆成 `data/questions/<分类>.ts` 8 个文件（各约 74 行）+ `index.ts` 组装。因 `moduleResolution: bundler`，6 处 import 无需改动。数据逐块比对，内容与顺序完全一致 |
| **本轮提交拆成 4 个** | 每个都在 800 行预算内（771 / 740 / 500 / 349），演示了行数门禁如何驱动拆分 |
| 文档同步 | 0.3 进度表、0.4 文件地图、0.6 技术债、0.7 下一步 |

首次克隆后需启用钩子：`git config core.hooksPath .githooks`
### A.8 v0.6 → v0.7

本轮为**工程化改动**：把代码纳入版本控制并推送到远程仓库。

| 变更 | 说明 |
| --- | --- |
| **建立 git 仓库** | `git init`（分支 `main`），远程为 `https://github.com/Htojk/ai_drill.git` |
| **新增 `.gitignore`** | 忽略 `node_modules/`（88.6 MB / 10410 个文件）、`app/dist/`（构建产物）、日志、编辑器与系统文件 |
| `app/dist` 不入库的理由 | 它是 `npm run build` 的产物，且部署由 `tcb hosting deploy` 直接从本地目录上传，无需经过仓库 |
| 文档状态更新 | 0.3 进度表、0.4 注意、0.6 技术债、0.7 下一步、README 均改为「已有 git 仓库」 |
| **未包含** | `cloudbase-418.md`（根目录调研残留）仍在，但已不属于代码；未做测试/CI |

### A.9 v0.5 → v0.6

本轮**有代码改动**，并同步更新文档。

| 变更 | 说明 |
| --- | --- |
| **补齐 A6：渲染出处原文片段** | `Quiz.tsx` 与 `WrongBook.tsx` 在出处链接下渲染 `snippet`（此前 24 题的片段数据全有、但从未显示），新增 `.snip` 样式 |
| **补齐 B4：题目反馈按钮** | 新增 `QuestionReport` 类型与 `aq.reports.v1` 存储；反馈区可选「答案有误 / 表述不清 / 有争议 / 其他」，已纳入进度导出码 |
| 反馈计数可见 | 「我的数据」页新增「题目反馈 N 条」 |
| **新增 `README.md`** | 项目入口，指向本文档第 0 章，含状态、启动与部署命令 |
| 文档同步 | 0.3 进度表、0.4 文件地图、0.6 技术债、0.7 下一步、A6/B4、7.2 数据模型、第 9 章、10.5 均按真实状态更新 |
| **标注线上待重新部署** | `dist` 哈希已变时，线上仍是旧版；当时已在 0.3、0.7、10.5 三处标出。**该状态已在 T-034 清零** |
| 验证方式 | `npm run build` 通过；用内置浏览器跑了真实交互：片段渲染 ✅、反馈写入 ✅、刷新后计数仍在 ✅ |

### A.10 v0.4 → v0.5

本轮**未改动任何代码**，只做文档纠偏与整理。

| 变更 | 说明 |
| --- | --- |
| **新增第 0 章「交接摘要」** | 明确写出项目目标、已定路线、当前进度、文件地图、已知问题、下一步；供后续 Agent 快速接管 |
| **修正 13 处功能状态不实** | A1/A6/A7/A9、B3/B4、D1/D2/D3/D4/D6/D8、E3 由「是」改为真实状态（部分 / 否），详见 0.6 |
| 修正失效交叉引用 | B6、E1 原指向 10.6（默认域名限制），实为 10.8（进度导出码） |
| 修正题库形态描述 | 第 7 章与 10.1/10.2 原称「编译成 `questions.json`」，实际是手写 TS 常量，无编译脚本 |
| 修正文件数 | 10.5 原记「上传 14 个文件」，实际 `dist` 为 8 个文件 |
| **修正失效的 418 结论** | 10.6 原记「所有路径返回 HTTP 418」，2026-09-22 复验实际返回 200，仅不存在路径 404 |
| 修正体积估算 | 500 题估算由 200–400 KB 改为约 750 KB（按 24 题 36 KB 实测推算） |
| 新增 iOS 图标风险 | 10.4 与第 12 章补充 iOS 不支持 SVG 图标的问题 |
| 里程碑标注实际达成 | 第 11 章 W1 标注「200 题 + 出题脚本」实际未完成 |
| 待确认问题标注已决 | 第 13 章 Q7/Q9 注明已由路线决策回答 |

### A.11 v0.3 → v0.4

| 变更 | 说明 |
| --- | --- |
| 技术路线定案 | 明确为「纯静态 Web PWA + 腾讯云 CloudBase 静态托管」，不再保留 Cloudflare 作为主计划 |
| 评估并否决微信小程序 | 小程序需 HTTPS 且已备案的 request 域名，个人主体类目受限；PWA 覆盖同等需求（调研见 `docs/research/小程序调研.md`） |
| 第 10 章重写 | 10.3 部署方案对比 → 已落地的单方案；新增 10.5 部署链路（已跑通）、10.6 默认域名硬限制、10.7 手机实测清单；章节由 9 节扩为 11 节 |
| **新增关键风险：CloudBase 默认域名中间页** | 默认域名会弹访问提示中间页，且官方仅建议用于开发测试，访问量异常会被风控关停；唯一合规的去除方式是绑定自定义域名 + ICP 备案（2–3 周） |
| 技术选型修正 | 由「Tailwind + shadcn/ui + Dexie」改为实际实现：原生 CSS + localStorage 封装 + TypeScript |
| 成本结论修正 | 删除「Cloudflare 永久 ¥0」等表述，改为按「自用 / 分发」两档说明 |
| 里程碑更新 | W4 改为部署到 CloudBase + 手机实测 + 按结果决定是否备案 |
| 风险表更新 | 删除「pages.dev 不可达」「CloudBase 6 个月后收费」两条，新增默认域名中间页、体验版额度与有效期两条 |

### A.12 v0.2 → v0.3

| 变更 | 说明 |
| --- | --- |
| 新增第 10 章「技术架构与免费部署方案」 | 架构原则、技术选型、三档部署方案对比、PWA 手机访问、国内可达性实测要求、已核实的免费额度、成本结论、落地顺序 |
| 架构原则新增 | 题库静态化 + 进度本地化 + 出题离线化 = 零服务器零数据库 |
| 技术选型调整 | 由 Next.js 改为 **Vite + React + Tailwind**；本地存储用 Dexie；新增 `vite-plugin-pwa` |
| 数据模型重构 | 拆分为"题库（构建期 JSON）"与"用户数据（IndexedDB）"两部分，删除 users/categories 等表 |
| 功能优先级调整 | 新增 A11 离线可答（P0）、B6 进度导出导入（P0） |
| 功能降级 | E1 账号体系 → MVP 不做；E3 线上管理后台 → 改为本地脚本工具；E4 埋点看板 → 不做 |
| 新增 D8 | 题库编译脚本（源数据 → questions.json） |
| 每日推荐算法 | 明确为纯前端计算，结果当天固定存 daily_tasks |
| 成功指标 | 新增"月度云成本 ¥0" |
| 里程碑 | W4 调整为 PWA 化 + 部署 + 手机 4G 实测 + 按需迁移 |
| 风险表 | 新增 4 条部署相关风险（国内不可达、CloudBase 收费、本地数据丢失、无账号无法同步） |
| 待确认问题 | 新增 2 条（是否接受无账号、手机运营商） |

### A.13 v0.1 → v0.2

| 变更 | 说明 |
| --- | --- |
| 新增第 2 章「竞品与差异化」 | 合并竞品调研结论，含竞品地图、关键竞品对比、市场空白、差异化主张 |
| 修正定位 | 明确"不做面试题库/求职突击"，改打"在职工程师的日常工程知识巩固" |
| 非目标新增一条 | 不做面试题库 |
| 新增第 5 章「题库分类体系」 | 从 F1 展开为独立章节，对标竞品覆盖度给出 9 大类 |
| 功能优先级调整 | B5 streak：P1 → **P0**；C2 掌握度图：P1 → **P0** |
| 新增题型 A2 | 工程场景判断题，并规定占比 ≥ 30% |
| 强化 A6 分层解析 | 明确要求"每个干扰项的错因"与"原文片段" |
| 数据模型 | 新增 `is_practice` 字段；question_sources 增加 spec 类型 |
| 每日推荐算法 | 新增"工程判断题 ≥ 3 题"的全局约束 |
| 题目来源 | 新增"竞品考点大纲作为覆盖面基准"（只抄覆盖面，不抄题） |
| 技术选型 | 新增可参考的开源实现清单 |
| 风险表 | 新增 4 条竞品风险 |
| 待确认问题 | 新增 2 条（小程序范围、工程题素材来源） |
